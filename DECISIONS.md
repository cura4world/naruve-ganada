# Naruve 발음앱 — 결정 기록

이 문서가 단일 출처(single source of truth)다.
전략서(.docx)와 충돌하면 **이 문서가 이긴다.** 전략서는 스냅샷이고 이 문서는 살아 있다.

작성 규칙
- 결론만 쓰지 말고 **기각한 선택지와 그 이유**를 함께 남긴다
- 추정이면 추정이라고 표시한다
- 뒤집힌 결정은 지우지 말고 취소선으로 남기고 아래에 새 결정을 적는다
- 새 항목은 해당 절 맨 아래에 날짜와 함께 추가한다

최종 갱신: 2026-08-19
문서 지문: 총 2093행 / 변경 이력 16건

---

## 0. 아직 열리지 않은 관문

**엔진 프로브가 끝나기 전에는 아래 결정을 확정하지 않는다.**

- 음절/어절 타일 결정은 아래 둘이 **모두** 충족될 때 닫는다.
  - (a) Syllables 배열이 안정적으로 오는가 — **부분 확인** (6/7, 2026-08-07)
  - (b) 감점이 실제로 오류가 있는 음절에 떨어지는가 — **부정 증거 확보** (2026-08-09)

  (b)는 s1_batchim 등 의도적 오류 샘플로 확인한다.
  **개수가 맞는 것과 위치가 맞는 것은 다른 문제다.** Syllables 개수 일치만
  보고 관문을 열면, 위치가 틀린 것으로 판명됐을 때 문장 확장·번역을
  되돌려야 한다. (2026-08-07)

  (b)에 반례가 나왔다. 같은 오디오·같은 구간의 음절 점수가 참조 텍스트의
  띄어쓰기만으로 76 ↔ 100으로 움직였다 (8.8 확정 3). 감점이 오류 위치에
  떨어진다는 전제가 적어도 이 사례에서는 성립하지 않는다.
  **프로브 결과만으로 이 관문을 닫을 수 없다.** 엔진을 더 재도 음절 점수가
  어절 분할에 따라 움직인다는 사실 자체는 바뀌지 않기 때문이다.
  별도의 제품 설계 논의가 필요하다. 그 논의는 아직 열리지 않았고 결론도 없다.
  (2026-08-09)

  2026-08-10 실측에서 (b)에 다른 성격의 반증이 하나 더 나왔다 (8.9 D군·B군 부수 발견).
  어제 것은 "같은 음절 점수가 참조 띄어쓰기에 따라 움직인다"였고,
  오늘 것은 두 가지다.
  - 의도적으로 틀리게 읽은 발화가 정상 참조로 98.2점을 받았다.
    감점이 오류 위치에 떨어지기는커녕 감점 자체가 거의 없었다.
  - 한 응답 안에 하위 층 점수가 상위 값의 복사본으로 오는 어절이 있다.
    그 어절에서는 음절 점수가 개별 측정값이 아니라 어절 점수의 복제다.

  표본은 화자 1인이다. 관문은 여전히 닫지 않고, 대안도 여전히 정하지 않는다.

  2026-08-10 REST 실측에서 (b)와 관련해 두 가지가 갱신됐다.
  - 8.8 확정 4("어절이 1개면 Syllables가 오지 않는다")가 반증됐다.
    Syllables 결측 조건은 어절 수가 아니라 자음 병합 쪽으로 수렴한다.
    폴백 규칙이 걸리는 자리가 바뀐다는 뜻이다.
  - EnableMiscue=false가 강제 정렬임이 실측으로 확인됐다(8.10).
    8.8·8.9의 Studio 측정은 전부 그 상태에서 나온 값이다.

  둘 다 (b)를 여는 근거도 닫는 근거도 아니다. 관문은 그대로 열려 있다.
  제품 설계 논의는 아직 열리지 않았고 결론도 없다.

  **2026-08-11~12 — 자체 채점 경로를 검증했다. 관문은 아직 닫지 않는다.**

  Azure 음절 점수와 별개로, MFA GOP + AI-Hub 정답지 학습으로 자체 음절 층을
  검증했다(8.12). 위치 적중 top-1 34.1%±0.6, top-3 61.6%(무작위의 3.9배).
  GOP 단독은 폐기, 학습 경로는 채택하되 **34%로는 단정 표시를 만들지 않는다.**

  규모 곡선이 꺾여 다음 검증은 자기지도학습 음향 표현이다(8.12.6).
  그 결과가 나오기 전에는 관문을 닫지 않는다.
  어절 타일(Azure)은 확인된 안전판으로 유지된다.

  **2026-08-13 — 신형 특징으로 top-1 44.1% (8.13). 초기 눈높이였던
  top-1 50%가 사정권에 들어왔다.** 서버 필요가 확정 수치로 나왔고(전량 CPU 14시간),
  GPU 실측과 다음 개선(미세조정 등) 결과를 보고 관문을 판정한다.
  어절 타일(Azure)은 여전히 안전판으로 유지.
- ~~오류 가설 재채점이 실제로 작동하는지~~
  ← **상정한 방향은 닫혔다 (2026-08-18).** "정답 가설로 바꾸면 점수가 튄다"는
  4/4 미작동으로 실측됐다(4절). 8.9와 합쳐 두 번째 같은 결과다.
  소거법 변형은 4절 안에 미결로 남아 있고, 실제 학습자 음성이 있어야 재볼 수 있어
  이 관문에는 걸어 두지 않는다.
- 무료(기본) 모드가 발음 채점을 할 수 있는지

남은 둘이 열려야 문장 확장·번역·UI 작업이 의미를 가진다.

---

## 1. 회사 · 계정 (확정)

| 항목 | 값 |
|---|---|
| 상호 | Naruve Studio / 나루브스튜디오 |
| 도메인 | naruve.app (구매 완료) |
| 앱 가칭 | 가나다라 (정식 미정) |
| 사업자 | 개인사업자, 본인 명의, 자택, 주업종 722000 |
| 플레이 계정 | 조직 계정, 표시명 Naruve Studio |
| 상표 | KIPRIS "naruve" 국내 0건 (2026.07 확인) |

진행 중: 사업자등록 → 발급 즉시 D-U-N-S 신청 (5~28영업일, 최대 병목)

---

## 2. 제품 성격

**학습앱이 아니라 점수앱.** 첫 화면이 곧 기능이다.
로그인·온보딩·레벨테스트를 앞에 두지 않는다.

핵심 기능
1. 발음 점수 (간판)
2. 존댓말 레벨 체커 (방어선)
3. 한자어 브릿지 — 일본·대만 전용

영어를 매개하지 않는다. 학습자 모국어로 직접 설명한다.

---

## 3. 채점 철학 — 2026-08-03 확정

### 3.1 기준은 "원어민다움"

**확정: 원어민다움(native-likeness).**
**기각: 알아들을 수 있음(intelligibility).**

이유
- GOP 계열 엔진은 애초에 원어민 통계와의 거리를 재는 구조다. 목표와 도구가 일치한다
- "알아들을 수 있게"는 경쟁 앱도 하는 애매한 약속이고, "원어민처럼"은 검증 가능한 약속이라 신뢰를 만든다

딸려오는 의무
- **표준 발음(서울) 기준임을 앱에 명시한다.** 지역 방언 화자인 한국인이 82점을 받는 상황이 초기 시딩에서 발생할 수 있고, 그 자리에서 신뢰가 무너진다

### 3.2 눈금과 기준은 별개다

혼동 주의:
- **기준을 원어민다움에 둔다** = 자의 100이 어디냐
- **원어민이 92점 이상 받는다** = 자가 제대로 만들어졌느냐

원어민이 78점을 받으면 그건 "엄격한" 것이 아니라 **고장 난 것**이다.
목표 분포: 원어민 92~100에 좁게 집중 / 학습자 30~90에 넓게 분산.

미측정 상태. 프로브에서 확인한다.

**화면 값과 JSON 값이 다르다 — 앱은 JSON을 받는다 (2026-08-09 실측)**

같은 응답인데 Speech Studio 화면은 유창성 32 / 발음점수 45를 보였고,
JSON은 FluencyScore 100 / PronScore 66이었다. 앱이 받는 것은 JSON이다.
**모든 판단은 JSON으로만 한다. 화면 숫자를 결정 근거로 인용하지 않는다.**

→ 인수인계 문서의 원어민 눈금 4개(100 / 98 / 95 / 91)가 화면에서 읽은 값인지
JSON인지 확인되지 않았다. 확인 전까지 이 절의 "원어민 92 이상" 판단 근거로
쓰지 않는다. **재측정 대상이다.**

**원어민 눈금 재측정 — 4개 중 1개 미달 (2026-08-10 실측)**

인수인계 문서의 4개 값은 화면(반올림)이었다. JSON으로 다시 쟀다.
화자 1인이 폰으로 녹음한 m4a다.

| 문장 | 인수인계 기록(화면) | PronScore(JSON) | AccuracyScore |
|---|---|---|---|
| 감사합니다 | 100 | 100 | 100 |
| 날씨가 많이 더워요 | 98 | 97.6 | 96 |
| 많이 드세요 | 95 | 94 | 90 |
| 만나서 반갑습니다 | 91 | 89.2 | 82 |

- 바로 위의 "재측정 대상"은 해소됐다. 네 값 모두 JSON이다
- **4개 중 1개(만나서 반갑습니다 89.2)가 이 절의 "원어민 92 이상"에 미달한다.**
  자가 제대로 만들어졌는지는 아직 확정되지 않았다
- 다만 이 문장의 녹음만 SNR 14.66이었다(나머지 21~33). 녹음 환경이 달랐으므로
  **재녹음 후 재측정 대상**이다. 환경 탓인지 눈금 탓인지 지금 값으로는 갈리지 않는다
- 표본은 화자 1인·문장 4개다. 목표 분포(원어민 92~100 집중)의 검증으로는 부족하다
- 전문은 8.9 A군

**정정 — "01번만 SNR이 낮았다"는 A군 안에서만 맞다 (2026-08-10)**

위의 "이 문장의 녹음만 SNR 14.66이었다(나머지 21~33)"는 **A군 4개 안에서만**
그렇다는 뜻이었는데 그렇게 쓰지 않았다. 같은 날 REST로 잰 다른 녹음에도 낮은
값이 있다 — `17 싸요` **14.70**이다.

같은 회차에서 읽은 값: 15 반갑습니다 21.09 / 16 얼마예요 28.42 / 17 싸요 14.70 /
18 좋네요 21.83 / 12 공부하고 있어요 24.86 / 13 갈 수 있어요 33.73.

따라서 **"01번만 녹음 환경이 달랐다"는 추정은 약해진다.** 다만 01번의 92 미달이
SNR 탓인지 눈금 탓인지 갈리지 않았다는 판단과 재측정 대상이라는 결론은 그대로다.
**바뀌는 것은 "01번만"이라는 서술뿐이다.**

**같은 JSON이라도 Speech Studio와 REST가 다르다 (2026-08-10 실측)**

같은 오디오·같은 참조 텍스트·같은 설정인데 두 경로의 응답이 다르다.
`17 싸요`의 PronScore가 **Studio 89.8 / REST 88.0**이다.

스키마도 다르다. Studio는 점수를 `PronunciationAssessment` 아래에 중첩해 주고
**REST는 평평하게 준다.** 최상위도 Studio는 리스트, REST는 dict다.

- **기준은 REST다.** 앱이 받는 것이 그쪽이다. Studio에 맞추지 않는다
- 절대값이 최대 1.8점 어긋나지만 8.5의 핵심 지표는 gap이고, 같은 경로로 재면
  공통 편차는 상쇄된다. Studio 내부 전처리(m4a 변환·리샘플링)는 우리가 알 수 없다
- **REST도 결정론적이다.** 같은 호출을 다른 임시 변환으로 두 번 불러 88.0으로
  같았다. 8.8 확정 1이 REST에서도 성립한다
- 이 절의 "모든 판단은 JSON으로만 한다"에 한 줄이 붙는다 —
  **어느 경로의 JSON인지도 적는다**

### 3.3 엔진은 "얼마나 다른가"까지만 산다

Azure가 주는 것은 "갑 34점"이라는 **증상**이다. **원인은 안 준다.**
받침 미실현 / 과도한 파열 / 다른 소리로 대치 — 셋 다 34점이 나오지만 교정 문구는 전부 다르다.

따라서 "원어민처럼 교정해 준다"는 약속은 **엔진을 사서 지킬 수 없고 직접 만들어야 한다.**
이 부분이 이 앱의 실제 제품이자 해자다.

### 3.4 무엇을 감점하고 무엇을 넘어갈 것인가 — 2026-08-11 확정

3.1이 "원어민다움"을 기준으로 삼았으나, 어느 수준까지를 원어민다움으로
볼지는 정해두지 않았다. GOP 검증 과정에서 음향모델의 음소가 자모보다
잘게 나뉘어 있어 이 선을 그어야 실험이 성립했다. 그래서 여기서 정했다.

#### 원칙

**한국인도 신경 쓰지 않는 차이는 넘어가고,
한국인 귀에 외국인처럼 들리는 차이는 감점한다.**

#### 직접 결정 여섯 (2026-08-11, 예시 기반으로 사람이 정함)

| # | 구분 | 예 | 결정 |
|---|---|---|---|
| ① | 모음 길이 | 눈(目) / 눈(雪) | **넘어간다** |
| ② | 구개음화 | 니의 ㄴ / 나의 ㄴ | **넘어간다** |
| ③ | 원순화 | 호의 ㅎ / 하의 ㅎ | **넘어간다** |
| ④ | 유성·무성 | 고기 → [고키] | **감점한다** |
| ⑤ | 받침 미파음 | 밥 → [바브] | **감점한다** |
| ⑥ | ㅐ / ㅔ | 개 / 게 | **넘어간다** |

근거 —
①②③⑥은 원어민 자신이 의식하지 않는다. 원어민도 안 지키는 것을
학습자에게 요구하면 이유 없는 감점이 된다.
- ①의 장단음은 현대 서울말에서 거의 소멸했다
- ②③은 뒤따르는 모음 때문에 자동으로 생긴다. 앞 자음을 제대로 내면 저절로 된다
- ⑥은 구분해서 낼 수 **있으나** 일상 발화에서 신경 쓰지 않는다.
  구분하려면 에너지가 들고, 그 수준을 요구하는 앱이 아니다

④⑤는 들으면 외국인임이 드러난다.
- ④ [고기]를 [고키]처럼 내는 것은 뜻은 통하나 한국인 귀에 어색하다
- ⑤ AI-Hub에서 종성 오류가 전체 태그의 11.07%다. 가장 흔한 오류군의 하나다

#### 연장 결정 넷 (2026-08-11, 위 원칙의 적용)

직접 결정이 아니라 여섯 결정의 원칙을 실측 잔여 항목에 적용한 것이다.
뒤집을 일이 생기면 여섯 결정과 구분해서 본다.

| 항목 | 결정 |
|---|---|
| 구개음 이형태 (ɕ ʑ ɲ ʎ 등) | ②의 연장 — 합친다 |
| ㅎ 이형태 (ɦ x ɣ β 등 8종) | ③의 연장 — 합친다 |
| 활음 (j w ɥ ɰ) | 뒤 모음과 묶어 이중모음 자모 하나로 — ㅑ, ㅘ 등 |
| 받침 중화 | 받아들인다. 옷·낮·꽃·밭의 받침이 같은 소리인 것은 한국어 규칙이다 |

**받침 중화와 ⑤는 충돌하지 않는다.** ⑤는 "터뜨렸는가"이고
중화는 "어느 글자인가"다. [바브]는 여전히 감점된다.

#### ④의 실행 범위

**앱에서는 작동한다. 검증만 못 한다.**
AI-Hub 기준 전사에 유성·무성 구분이 없다(어두든 모음 사이든 `ㄱ`).
결정은 유지하고, 검증은 별도 표본이 생길 때까지 보류한다.

#### 알려진 한계

`ㅐ`와 `ㅔ`를 서로 바꿔 쓴 오류는 잡지 못한다.
음향모델에 `e` 하나만 있다. ⑥의 결정과 방향이 같으므로 문제로
취급하지 않되, 기능 한계로 기록해 둔다.

---

## 4. 오류 가설 재채점 (2026-08-18 실측 — 상정한 방향으로는 미작동)

**2026-08-18 실측 결과.** 산출물은 `D:\aihub_work\rescore_probe_20260818\`
(`summary.md` · `rescore_table.csv` · `raw/` 응답 12건. 저장소 밖).
의도적 오류 발화 4개(d15~d18, 각 1어절, 원어민 화자 흉내)에 참조 텍스트 3벌
— (a) 정답 / (b) 정답 가설 / (c) 오답 가설 — 을 넣어
Azure REST(koreacentral, EnableMiscue=false)로 12회 호출했다.

- **정답 가설로 바꿔도 점수가 오르지 않았다.** (b−a)가 문장 −2.4~+2.4,
  단어 −4~+4. "튐"(≥10점) **0/4**이다.
  8.9의 최대 2.4점과 같은 결과가 두 번째로 나왔다.
- **오답 가설로 바꾸면 점수가 크게 떨어졌다.** (c−a) 단어 점수가
  d17 −18, d18 −12. d15·d16은 무반응이었다.

~~정답 가설을 넣으면 점수가 튄다 → 튀는 가설이 오류~~ ← 기각 (2026-08-18)
  4/4 미작동. 8.9와 합쳐 두 실측이 같은 방향이다.
  첫 버전 유료 층·오류 설명의 근거로 쓰지 않는다.

**미결 — 소거법 변형.** "여러 가설 중 점수가 **떨어지지 않는** 것이 실제 발음"으로
뒤집으면 성립할 가능성이 2/4에서 보인다. 그러나 발화 4개·1어절·화자 1명이라
판단할 수 없다. 실제 학습자 음성이 필요하다. AI-Hub는 8.11.6으로 외부 API 전송이
금지이고 우리 로그는 출시 후에 생긴다.
**비공개 테스트 테스터 음성(선택 동의 층)이 첫 재료다.**

**재채점의 남은 자리 (2026-08-18).** 실시간 사용자 기능이 아니라 **오프라인 라벨
도구** 후보로 둔다 — 저장된 로그의 표본(월 2~5%, 실측 후 조정)에 배치로 돌려
우리 모델(15절)의 학습 라벨을 만드는 용도다. 소거법 변형이 확인된 뒤에만 착수한다.
실시간으로 전 사용자에게 열면 원가가 가설 수+1배(3가설이면 4배)가 되어
12개월 추정에서 원가가 매출을 넘는다(17.8).

아래는 그대로 둔다. 설계가 무엇이었는지가 남아 있어야 같은 제안이 다시 올라오지 않는다.

### 4.1 방식

사용자 오디오는 그대로 두고 **참조 텍스트만 바꿔** 재채점한다.
점수가 튀는 가설이 실제 오류다.

예: "반갑습니다"가 34점일 때

| 참조 텍스트 | 점수 | 해석 |
|---|---|---|
| 반갑씀니다 (정답) | 34 | 증상 |
| 반가씀니다 (받침 탈락 가설) | **91** | ← 원인 확정 |
| 반갑슴니다 (경음화 실패 가설) | 40 | 아님 |

→ 교정 문구: "받침 ㅂ이 사라졌습니다. 입술을 닫되 터뜨리지는 마세요."

### 4.2 원가

8초 평가 약 4원(추정, 공개 단가 기준). 가설 2개 추가 시 약 12원.
**전체 점수가 낮을 때, 최저 어절 하나에만** 적용하면 평균 1.3배 수준.

### 4.3 미검증 전제

- Azure ko-KR이 어절 단위 점수를 안정적으로 주는가
- 참조 텍스트를 비표준 표기로 넣어도 정상 처리하는가
- 가설이 맞을 때 점수가 실제로 유의미하게 튀는가

**이 셋이 확인되기 전까지 오류 가설 사전 작성에 착수하지 않는다.**

**딸려오는 제약: 가설은 어절 분할을 고정한 채로만 세운다 (2026-08-09)**

띄어쓰기만 바꿔도 같은 발화의 PronScore가 66 ↔ 97.6으로 움직인다(8.8).
가설 재작성이 어절 경계를 건드리면 점수 급등이 **가설 적중인지 어절 분할 효과인지
분리되지 않는다.** 가설 참조 텍스트는 원본과 어절 수·경계를 똑같이 유지한다.
4.1의 `반갑씀니다` → `반가씀니다`는 이 조건을 만족한다(둘 다 2어절, 경계 동일).

**전제 3번은 Azure ko-KR에서 부정 쪽으로 실측됐다 (2026-08-10)**

의도적으로 틀리게 읽은 녹음에 정상 참조와 오류 참조를 각각 넣었다(8.9 D군).

| 녹음(실제 발음) | 정상 참조 | 오류 참조 | 차 |
|---|---|---|---|
| [반가씀니다] | 반갑습니다 98.2 | 반가씀니다 97.0 | -1.2 |
| [올마예요] | 얼마예요 95.2 | 올마예요 92.2 | -3.0 |
| [사요] | 싸요 89.8 | 사요 91.6 | +1.8 |
| [조은네요] | 좋네요 91.6 | 조으네요 94.0 | +2.4 |

가설이 맞을 때 점수가 튀어야 하는데 **최대 3.0점이고 부호도 갈렸다.**
**미검증 전제 3번은 Azure ko-KR에서는 성립하지 않는 것으로 측정됐다.**
표본은 화자 1인·4쌍이고, 오류는 TTS가 아니라 사람이 낸 것이다.

위의 착수 금지는 그대로 유효하다. 4절 자체의 처리는 오늘 결정하지 않는다.

---

## 5. 오류 가설 사전 (착수 전)

### 5.1 정의

되물어볼 가설은 무한하므로 **어느 것부터 물을지 정한 모국어별 우선순위표**가 필요하다.
이것이 앱의 실질적 지적재산이다.

### 5.2 초판은 데이터 없이 만들 수 있다

모국어 음운체계와 한국어의 대조만으로 상당 부분 예측 가능하다(대조분석). 실측으로 순위를 조정한다.

인도네시아어 예측 (초안, 미검증)

| 예측 오류 | 근거 |
|---|---|
| ㄱ/ㄲ/ㅋ 미분화 | 인니어 파열음은 2중 대립(유무성), 한국어는 3중 |
| ㅓ·ㅡ 미분화 | 둘 다 인니어 ə 하나로 수렴 |
| 어두 유성음 과다 | 인니어 유성음이 뚜렷 |
| 받침은 양호 | 인니어에도 미파 종성 존재 |

일본어는 반대로 받침이 최대 난점(ㄴ 계열 외 종성 부재 → 모음 삽입).
태국어는 3중 대립이 있어 ㄱ/ㄲ/ㅋ이 오히려 쉽다.

→ **같은 문장에 대해 모국어마다 다른 순서표가 필요하다.**

### 5.3 표본

인도네시아 사역 + 다국적 이주민센터 사역 겸임.
베트남·태국·우즈베키스탄·네팔·필리핀 등 확보 가능.

방법: 별도 녹음 세션을 열지 않는다. **베타 테스터를 국적별로 고르게 뽑아** 실사용 데이터로 대체한다.

윤리: 이주민은 제도적 취약 위치에 있고 목회자와의 관계에 거절하기 어려운 비대칭이 있다.
"참여하지 않아도 무방하다"를 명시적으로 고지하는 절차를 반드시 둔다.

---

## 6. 점수 표시 — 2026-08-03 확정

### 6.1 구조

절대 점수는 정직하게 유지하고, 공유 동기는 옆에 붙인다.

```
55점  (원어민 기준)
인도네시아 학습자 중 상위 15%
어제보다 +9
받침 ㅂ — 이 문장 완성  🏅
```

### 6.2 함정 세 가지와 대응

| 함정 | 대응 |
|---|---|
| 첫 세션에는 비교 대상이 없다 | 부분 성취(배지)로 채운다 |
| 사용자가 늘면 백분위가 저절로 하락 | 실시간이 아닌 **고정 기준 집단**(초기 500명 분포)에 대고 계산. 달성 등급은 배지로 고정, 하락 없음 |
| 전체 비교는 불공정하고 숫자도 낮다 | **같은 모국어끼리** 비교한다 |

마지막 항목은 다국적 표본이 있어야만 가능한 설계다.

---

## 7. 모국어 선택

**국적이 아니라 모국어를 받는다.** 오류 패턴은 소리 체계에 붙지 여권에 붙지 않는다.
(재한 중국동포, 자바어 모어 인도네시아인 등 국적≠모어 사례 다수)

**별도 화면을 만들지 않는다.** UI 언어 선택이 곧 모국어 선택이다.
기기 언어로 초기값을 잡고 사용자가 변경 가능하게만 한다.
→ 온보딩 무배치 원칙과 충돌하지 않는다.

---

## 8. 엔진 (미확정)

### 8.1 중요한 사실 — 참조 오디오는 채점에 안 쓰인다

Azure 발음평가 scripted 모드는 **참조 텍스트만** 받는다.
채점은 "사용자 음성 vs 원어민 음성 파일"이 아니라 "사용자 음성 vs 텍스트 유도 음향모델" 비교다.

| 층 | 채점 기준 | TTS의 역할 |
|---|---|---|
| 발음 (Azure/ETRI) | 참조 **텍스트** | 채점 무관. 듣기용 자산 |
| 억양 (자체 pitch.js) | 참조 **오디오 F0** | **여기서는 TTS가 실제 채점 기준** |

→ TTS 벤더 선정 기준은 "목소리가 예쁜가"가 아니라 **"의문문/평서문 억양이 한국어답게 갈리는가"**다.

**ProsodyScore는 ko-KR에 오지 않는다 — 실측 확인 (2026-08-07)**

Speech Studio 발음평가 도구에서 **Prosody assessment를 켜고** ko-KR로 4문장을 돌렸으나, 응답의 문장 단위 `PronunciationAssessment`에는 `AccuracyScore` / `FluencyScore` / `CompletenessScore` / `PronScore` 넷뿐이었다. 4개 샘플 전부 없음. **화면에 옵션이 있어도 ko-KR 응답에는 오지 않는다.**

- 8.2 표의 "운율 en-US만"이 추정에서 실측으로 바뀌었다
- 억양 층은 `pitch.js`가 계속 맡는다(10절). 엔진 선택과 무관하게 자체 구현이 기본이다
- 뒤집어 말하면 억양 층은 Azure를 끊어도 죽지 않는다. 원가 0의 무료 기능이라는 10절의 전제가 유지된다

### 8.2 후보

| 후보 | 강점 | 약점 |
|---|---|---|
| Azure PA | 유일한 성숙 상용 API, 저렴, 문서 충실 | **영어 우선 설계의 한국어 이식.** 음소 이름 en-US·zh-CN만, 음절 그룹 en-US만, 운율 en-US만. 한국어 정확도 미공개 |
| ~~ETRI 발음평가~~ **탈락 (2026-08-10)** | ~~**외국인의 한국어 발음평가를 명시적 용도로 표방.** 무료, REST~~ | **응답이 `recognized`와 1~5점 `score` 둘뿐이다.** 어절·음절·음소·Offset이 없다. 아래 참조 |
| 온디바이스 (Web Speech) | 원가 0 | LM이 나쁜 발음을 교정해 뱉음 → 채점 불가 가능성 높음 |
| 자체 구축 (GOP) | 통제권·원가 | ML 엔지니어링 부담. 사용자 축적 후의 과제 |
| CrisperWhisper 2.0 (nyrahealth) | 오픈 웨이트. verbatim/intended 모드 분리로 ASR의 교정 편향을 억제하는 설계. 정확한 단어 타임스탬프. 자체 호스팅 시 호출 원가 0 | verbatim이 음소 충실도를 뜻하는지 미확인(비유창성 보존을 뜻할 가능성). 한국어 지원 여부 미확인. GPU 운영 부담 |
| 자체: 신형 특징(w2v2 음소 CTC) + 학습 판별기 | top-1 44.1% / top-3 67.2%. AI-Hub 정답지 학습. 3.4 ④⑤ 구분이 모델 출력에 내장 | 서버 필수(315M, CPU 1.3초/건). 단정 표시엔 아직 부족. GPU·미세조정 검증 대기 (8.13) |

구글·아마존은 발음평가 제품 자체가 없다.

**기각: Grok Voice Think Fast 2.0** (2026-08-04)
speech-to-speech 대화 에이전트라 음소 점수를 주지 않는다.
전사 정확도가 높을수록 오히려 학습자 오류를 교정해 버려 D-09의 문제를 그대로 안는다.
분당 $0.08로 원가도 Azure의 4배.
(추후 회화 상대 기능 검토 시 재검토)

**탈락: ETRI 발음평가 (2026-08-10)**

aiopen.etri.re.kr은 2025-06-30 운영을 종료했고 서비스는 e-PreTX(epretx.etri.re.kr)로
이관됐다. 가입과 API 키 발급을 마치고 한국어 발음평가 API 사양을 확인했다.

| 항목 | 값 |
|---|---|
| 엔드포인트 | `http://epretx.etri.re.kr:8000/api/WiseASR_PronunciationKor` (http, 포트 8000) |
| 요청 | `argument { language_code: "korean", script, audio(base64) }` |
| 헤더 | `Authorization: ACCESS_KEY` |
| 허용량 | 1,000건/일, 건당 60초 이내 |
| 응답 | `recognized`(발음평가 결과) / `score`(1~5점) — **두 필드뿐** |

어절·음절·음소·Offset이 전부 없다. 문장당 1~5점이 전부다.

- **음절 타일을 그릴 수 없다** (8.6)
- **오류 가설 재채점을 할 수 없다.** 5단계 척도로는 참조 텍스트를 바꿨을 때의
  점수 변화를 읽을 수 없다 (4절)
- **0~100 점수 표시의 원 데이터가 되지 못한다** (3.2)

Azure는 "진단은 못 해도 채점은 100점 척도"였으나 ETRI는 채점 해상도 자체가 5단계다.
**프로브를 돌릴 필요가 없다. 응답 규격만으로 탈락이다.**

egress 허용목록 항목은 14절에 남겨둔다.

### 8.3 한국어 고유 난점 (엔진 무관)

1. **평음·경음·격음 3중 대립** — 음향적으로 미묘하고, 학습자가 가장 많이 틀리는 지점
2. **받침 미파음** — "제대로 안 터뜨림"과 "아예 안 냄"이 둘 다 저에너지. 원어민 데이터로 학습한 모델은 생략을 벌점 줄 줄 모를 수 있음
3. **음운 변동 G2P** — 텍스트→음소 변환이 틀리면 이후 전부 무의미

### 8.4 검증 방법 — 의견이 아니라 측정으로

**AI-Hub「교육용 영어 모국어 사용자의 한국어 음성 데이터」**
음소 단위 발음오류 태깅(JSON·TextGrid) + 사람 채점 결과 포함.
= **전문가 정답지.** ~~여기에 Azure·ETRI를 통과시켜 사람 점수와의 일치도를 측정한다~~
  ← 정정 (2026-08-10). ETRI가 탈락해 통과시킬 대상이 아니다.
    AI-Hub 정답지를 자로 쓴다는 방침 자체는 유효하다. 잴 엔진 목록만 비었다.

한계: 화자가 영어 모어라 인도네시아어권 패턴과 다르다.
이점: 내국인만 접근 가능 → **해외 경쟁사는 이 검증 자체를 못 한다.**

방침: **엔진을 고르지 말고 세 개를 같은 자로 재고 나서 고른다.**
프로브 하네스는 모양이 같으므로 엔진 추가 비용이 거의 없다.

**2026-08-10 — 대안 엔진 측정의 우선순위가 올라간다.**
4절 오류 가설 재채점이 Azure ko-KR에서 작동하지 않는 것으로 측정됐다(4.3 · 8.9 D군).
재채점은 이 앱의 교정 문구가 서 있는 구조이므로, 그것이 안 되는 엔진 하나만 재고
결론을 낼 수 없다. **어느 엔진으로 간다는 결론은 아직 없다.** 측정 순서만 바뀐다.

**단서 — 잴 대상이 하나 줄었다 (2026-08-10)**

ETRI 발음평가가 응답 규격만으로 탈락했다(8.2). 응답이 `recognized`와 1~5점 `score`
둘뿐이라 어절·음절·음소가 없고, 애초에 같은 자에 올릴 수 있는 값이 아니다.
그래서 이 후보는 프로브를 돌리지 않고 빠진다.

**방침 자체는 그대로다.** 고르기 전에 재고, 재고 나서 고른다.
다만 바로 위에서 우선순위를 올린 **대안 엔진 측정의 대상 하나가 그 직후 빠졌다.**
**새 후보를 정하는 것은 오늘 하지 않는다.**

8.5의 "엔진을 바꾸면 같은 문장으로 다시 잰다"도 그대로다.
ETRI 하나가 빠진 것이지 재는 방식이 바뀐 것이 아니다.

### 8.5 프로브 세트 설계

**표기형 vs 표준발음형 — 확정: 표준발음형 (2026-08-07 실측)**

~~같은 오디오에 참조 텍스트만 바꿔 2회 호출하고 "많이" 어절의 음소 배열 길이를 비교한다.~~
~~`많이 드세요` vs `마니 드세요` → 길이가 다르면 표기형 기준, 같으면 표준발음형 기준.~~
← **기각.** 절차를 밟기 전에 실측으로 답이 나왔다.

Speech Studio ko-KR 응답에서 "많이" 어절의 `Phonemes`가 **4개**로 왔다.
표기형이면 ㅁㅏㄴㅎㅣ 5개, 표준발음 [마니]면 ㅁㅏㄴㅣ 4개다. ㅎ 탈락이 반영된 상태로 채점한다.
음절 분할도 발음 기준이라 "많이"가 [마][니]로 나뉜다.

- **참조 텍스트는 표기형을 그대로 보낸다. G2P로 표준발음형을 만들 필요가 없다.**
  8.3의 난점 3번(음운 변동 G2P)이 Azure를 쓰는 한 우리 몫이 아니다
- 2회 호출 판별 절차와 `data/probe_set.json`의 `s2_alt_ref` 항목은 제거했다.
  `audio_from` 기능 자체는 하네스에 남는다 — 4.1 오류 가설 재채점이 같은 구조다
- 유의: 이것은 **Azure ko-KR의 동작**이다. ETRI가 같다는 보장은 없다. 엔진을 바꾸면 같은 문장으로 다시 잰다

~~"반갑습니다"의 음소 개수로 역산~~ ← **기각.** 경음화(ㅅ→ㅆ)·비음화(ㅂ→ㅁ)는 소리만 바뀌고 개수가 그대로다. 표기형 13, 표준발음 13으로 동일해 판별되지 않는다.

**오류 샘플은 표기를 비틀어 TTS로 생성한다** (9절 참조)

| id | 참조 텍스트 | TTS 입력 | 오류 유형 |
|---|---|---|---|
| s1_ok / s1_batchim | 만나서 반갑습니다 | `만나서 반갑습니다` / `만나서 반가씀니다` | 받침 탈락 |
| s2_ok | 많이 드세요 | `많이 드세요` | 표준발음형 확정의 근거 문장 |
| s3_ok / s3_vowel | 이거 얼마예요? | `이거 얼마예요?` / `이거 올마예요?` | ㅓ→ㅗ |
| s4_ok / s4_tense | 값이 싸요 | `값이 싸요` / `값이 사요` | 된소리→예사소리 |

한 문장에 두 오류를 넣으면 어느 쪽이 감점됐는지 분리되지 않으므로 문장을 나눈다.

**핵심 지표는 절대 점수가 아니라 gap이다.** 정상 샘플과 오류 샘플의 점수 차, 그리고 감점이 실제로 오류가 있는 어절에 떨어졌는지. 엉뚱한 어절이 깎이면 점수가 낮게 나와도 실패다.

**벤더 편향:** Azure TTS 음성을 Azure 발음평가에 넣으면 같은 회사 음향 특성이라 점수가 부풀 수 있다. 비-Azure TTS와의 점수 차로 편향 크기를 측정한다.

- **CrisperWhisper 검증**은 s1_batchim 오디오를 넣어 `반가씀니다`로 적는지
  `반갑습니다`로 적는지만 보면 된다. 전자면 D-09의 기본 모드 후보,
  후자면 Web Speech API와 동일해 기각. 별도 설계 불필요. (2026-08-04)

**참조 텍스트의 띄어쓰기를 세트에 고정한다 (2026-08-09)**

띄어쓰기가 표기 규범이 아니라 채점 파라미터이므로(8.8), 위 표의 표기를 **글자 그대로**
보낸다. 어절을 임의로 붙이거나 떼면 그 회차는 다른 회차와 비교할 수 없는 값이 된다.
응답의 `Words` 개수를 결과에 함께 기록하고, 참조 어절 수와 다르면 표에 표시한다.

**같은 호출을 반복하지 않는다.** Azure ko-KR은 결정론적이다(8.8 확정 1).
반복 측정은 같은 값을 다시 사는 것이다. 편차 확인 목적의 재호출 항목을 넣지 않는다.

**이 세트로 D군을 실측했다 — 세트의 유효성은 미확정 (2026-08-10)**

위 표의 오류 샘플 설계(s1_batchim / s3_vowel / s4_tense와 같은 꼴)를 그대로 써서,
TTS가 아니라 사람이 직접 틀리게 읽은 녹음으로 4쌍을 측정했다(8.9 D군).
**정상 참조와 오류 참조의 gap이 최대 3.0점이고 부호도 갈렸다.**

"핵심 지표는 절대 점수가 아니라 gap이다"라는 이 절의 설계가 성립하려면
gap이 충분히 커야 한다. 3점은 그 조건을 만족하지 못한다.
따라서 **이 세트를 엔진의 판별력을 재는 자로 쓸 수 있는지 자체가 미확정이다.**

세트를 고치지도 폐기하지도 않는다. gap이 작은 것이 세트 탓인지 엔진 탓인지
아직 갈리지 않았고, 그것을 가르는 방법이 8.4의 "같은 자로 여러 엔진"이다.
표기를 비튼 TTS 샘플에서 같은 결과가 나온다는 보장도 없다.

### 8.6 타일 단위 — ~~어절 우선 (잠정)~~ → 음절 기본 + 어절 폴백 (2026-08-07 개정) → **음절 기본은 보류 (2026-08-09)**

~~Azure ko-KR은 음절 그룹을 주지 않고 음소 이름도 빈 문자열로 올 가능성이 높다(ja-JP 사례 기반 추정).~~
~~v1: **어절 단위 타일.**~~
← 전제 절반이 실측으로 뒤집혔다.

**틀린 전제:** "음절 그룹을 주지 않는다"는 추정이었다. 실측에서 **`Syllables` 배열은 온다.**
7어절 중 6어절에서 개수가 표기 음절 수와 정확히 일치했다
(감사합니다 5/5, 저는 2/2, 한국사람입니다 7/7, 날씨가 3/3, 많이 2/2, 더워요 3/3).

**맞은 전제:** 음소·음절 **이름은 전부 빈 문자열**이다. 이름표로 정렬할 수 없다.
다만 개수가 맞으면 순서 정렬은 성립하고, `Offset`이 층마다 붙어 있어 대조도 된다.

**새 결정**

1. **음절 타일을 기본으로 한다.** 단, 그 어절의 `Syllables` 배열이 존재하고 개수가 표기 음절 수와 같을 때만
2. **없거나 개수가 어긋나면 그 어절만 어절 단위로 폴백한다.** 한 화면에 두 단위가 섞이지만, 틀린 위치에 잉크를 채우는 것보다 낫다
3. **타일은 참조 텍스트가 아니라 응답의 `Words`를 기준으로 그린다.** 실측에서 "저는 한국사람입니다"가 `Words` 2개로 왔고 "한국사람입니다"가 통째로 한 어절이 됐다(그 어절 59점 / ErrorType Mispronunciation / 문장 CompletenessScore 50).
   ~~Azure는 참조 텍스트의 띄어쓰기를 그대로 따르지 않고 자체 판단을 섞는다.~~
   ← **정정 (2026-08-09).** 자체 판단이 아니었다. 그때 참조 텍스트가 `한국사람입니다`라는
   붙여쓰기였고 Azure는 그것을 그대로 따랐을 뿐이다. 8.8의 A/B/C가 이를 보인다
   (`한국사람입니다` → Words 2, `한국 사람입니다` → Words 3, `저는한국사람입니다` → Words 1).
   **결론은 유지하고 이유만 바꾼다.** Azure가 제멋대로 나눠서가 아니라,
   **참조 텍스트에 우리가 쓴 띄어쓰기가 곧 타일 격자이고 그 격자가 점수를 31점 흔들기 때문에**
   응답을 받아서 그리는 순서여야 한다
4. 원칙은 그대로: **정확한 정보를 덜 세밀하게 주는 쪽이, 세밀한 정보를 틀리게 주는 쪽보다 낫다.** 1~3은 이 원칙의 적용이지 예외가 아니다

**새 결정 1은 보류한다 (2026-08-09)**

"음절 타일을 기본으로 한다"의 근거는 Syllables 개수 일치였다. 개수는 여전히 맞는다.
그러나 8.8 확정 3에서 반증 사례가 나왔다 — 같은 오디오의 같은 `사`(Offset·Duration 동일)가
참조 띄어쓰기만 바뀌자 76 ↔ 100, 해당 음소는 53 ↔ 100으로 움직였다.
**음절 점수가 어절 분할에 따라 움직이는 이상, 개수 일치는 타일을 켤 근거가 되지 못한다.**

결정을 뒤집지 않고 보류로 표시한다. 대안은 아직 정하지 않았다.

**아직 확정되지 않은 것.** 개수가 맞는다고 감점이 맞는 음절에 떨어진다는 뜻은 아니다.
검증법은 기존과 같다 — 한 음절만 의도적으로 뭉갠 샘플에서 감점이 그 음절에 정확히 떨어지는지.
프로브 (C)가 지금 어절 단위로 그것을 재고 있고, 음절 단위 확인은 그다음이다.
**그때까지 음절 타일을 실제 화면에 켜지 않는다.**
사용자는 잘못된 지적을 검증할 수 없고, 잘못된 지적을 신뢰하게 만드는 것이 가장 나쁜 실패다.
0절 관문의 (b)가 이것이다.

**보류 근거 하나 추가 — 하위 층 점수가 상위 값의 복사본으로 온다 (2026-08-10 실측)**

10번 "어제 시장에서 과일을 조금 사 왔어요"에서 `조금`이
어절 60 / 음절 60·60 / 음소 5개 전부 60으로 왔다.
같은 응답의 `시장에서`는 전부 98, `과일을`은 전부 96이다.
반면 02번 `날씨가`는 어절 98 / 음절 89·100·100으로 층마다 값이 다르다.

**어떤 조건에서 층이 복사되고 어떤 조건에서 개별 측정되는지 미확정이다.**
복사된 어절에서 음절 타일은 어절 타일과 같은 정보를 세 배 세밀해 보이는 모양으로
그릴 뿐이고, 사용자는 그 차이를 구별할 수 없다.
음절 기본 보류의 근거가 8.8 확정 3에 이어 둘이 됐다.
표본은 화자 1인·문장 10개다. 14절에 이슈로도 올린다.

### 8.7 NBestPhonemes — 이름은 없지만 점수는 온다 (2026-08-07 관찰)

`Phonemes[].PronunciationAssessment.NBestPhonemes`에 후보가 5개씩 오고,
`Phoneme` 문자열은 전부 비어 있지만 **`Score`에는 값이 있다.**

- 2순위 점수가 높으면(실측에서 본 범위 50~80) 그 자리에서 소리가 흔들렸다는 신호로 읽을 수 있다.
  **어느 소리와 혼동됐는지는 이름이 없어 알 수 없다**
- 용도는 4절 오류 가설 재채점의 **보조 신호**다. *어디를* 되물을지 좁히는 데 쓰고,
  *무엇을* 되물을지 정하는 데는 쓰지 못한다. 후자는 5절의 모국어별 우선순위표 몫이다
- 되물을 자리를 좁힐 수 있으면 4.2의 재채점 호출 수가 줄어 원가가 내려간다
- **추정이다.** 2순위 점수와 실제 오류의 상관은 아직 재지 않았다.
  재는 방법: 오류 샘플의 target 어절에서 NBestPhonemes 분포가 정상 샘플과 다른지.
  프로브 원본 응답에 이미 다 들어 있으므로 추가 호출이 필요 없다
- 이 값은 **요청해야 온다.** PA 설정에 `NBestPhonemeCount`를 넣는다(기본 5).
  키 표기는 요청 규격이라 아직 미검증이다

### 8.8 참조 텍스트의 띄어쓰기는 채점 파라미터다 (2026-08-09 실측)

같은 오디오(저는한국사람입니다.m4a)에 참조 텍스트만 바꿔 4회. 전부 JSON 값이다.

| | A 한국사람입니다 | A' 동일 재실행 | B 한국 사람입니다 | C 저는한국사람입니다 |
|---|---|---|---|---|
| Words | 2 | 2 | 3 | 1 |
| AccuracyScore | 80 | 80 | 96 | 92 |
| CompletenessScore | 50 | 50 | 100 | 100 |
| PronScore | 66 | 66 | 97.6 | 95.2 |
| Syllables | 있음 | 있음 | 있음 | **없음** |

**확정**

1. **재실행 편차 0.** A와 A'가 Id만 다르고 소수점까지 동일하다. Azure ko-KR은 결정론적이다.
   → 프로브에서 같은 호출을 반복할 이유가 없다(8.5).
   10절의 재시도 결과 캐시도 UX를 왜곡하지 않는다 — 캐시가 답해도 사용자가 보는 값이 같다
2. **같은 발화가 66 ~ 97.6점.** 띄어쓰기는 표기 규범이 아니라 채점 파라미터다
3. **음절 점수는 실제 발음의 반영이 아니다.** 같은 Offset·Duration의 `사`가
   A에서 76, B에서 100이고 해당 음소는 A 53 / B 100이다.
   소리는 동일한데 어절 분할만 달랐다
4. ~~**어절이 1개면 `Syllables` 배열이 오지 않는다**(C). 14절의 "만나서" 사례(자음 병합)와
   원인이 다른 새 조건이다~~
   ← **반증 (2026-08-10 REST 실측).** `Words`가 1개인 응답 16건 중 **14건에
   배열이 정상으로 왔다**(반갑습니다 5 / 얼마예요 4 / 싸요 2 / 조으네요 4).
   없는 것은 `좋네요` 2건뿐이다. **어절 수는 조건이 아니다.**
   `좋네요`는 ㅎ+ㄴ, 14절의 `만나서`는 ㄴ+ㄴ으로 둘 다 자음이 이어지는 자리라
   **자음 병합 쪽으로 수렴한다. 확정하지는 않는다** — 표본이 화자 1인의 두 문장이다
5. **CompletenessScore = 오류 없는 어절 수 ÷ 참조 어절 수.** 어절이 적을수록 1건 실패의
   타격이 크다(2어절에서 1건 실패 → 50). PronScore 31점 폭의 대부분이 여기서 나왔다

**미확정 — 추측하지 않는다**

- 어절 AccuracyScore 산출 방식. 음절·음소 평균으로도 어절 길이로도 설명되지 않는다
  (A 7음절 59 < C 9음절 92)
- 이 현상의 일반성. 표본은 1문장이다

**폐기된 추정 — 되살리지 않는다**

- ~~`사` 53점 = 경음화 ㅆ 미실현~~ ← B에서 같은 소리가 100. 발음 현상이 아니라 어절 분할의 부산물
- ~~유창성 32가 감점 주범~~ ← 화면 값이다. JSON은 100 (3.2 참조)
- ~~어절이 길수록 불리하다~~ ← C(9음절 92) > A(7음절 59)로 반증

**방향성 정정 — "띄우면 오른다"는 규칙이 아니다 (2026-08-10 실측)**

같은 오디오에 참조만 붙임/띄움으로 바꿔 4문장 8회. 전부 JSON 값이다.

| 문장 | 붙임 | 띄움 |
|---|---|---|
| 한국 사람입니다 | 95.2 | 92.8 |
| 공부하고 있어요 | 94.6 | 85.0 |
| 갈 수 있어요 | ~~100~~ | ~~48.0~~ |
| 먹어 본 적 없어요 | 94.0 | 94.6 |

**`갈 수 있어요` 행은 화면 값이다. 근거로 쓰지 않는다** — 8.9 C군 정정 참조.
나머지 세 행은 JSON이다.

**4개 중 3개에서 붙임이 더 높다.** 위 A/B(띄움 97.6 > 붙임 66)와 반대 방향이다.

- **확정 2는 유지한다.** 띄어쓰기가 점수를 흔든다는 사실 자체는 그대로다.
  ~~(100 → 48)~~ ← 화면 값이었다. REST 실측에서 폭은 **최대 9.6점**이다.
  바뀐 것은 방향이다
- 8.8을 A/B 표만 보고 "띄우면 오른다"로 읽을 수 있으나 그 방향은 고정이 아니다.
  C군에서 붙임이 유리한 사례가 나왔다(Studio 3건 / REST 2건)
- 실제 변수로 보이는 것은 **참조의 어절 경계가 화자의 실제 끊어 읽기와 일치하는가**다.
  붙임/띄움 자체가 아니다. **추정이다** — 끊어 읽기를 따로 측정하지 않았고 표본은 4문장이다
- 위험 지점은 **정서법과 발음 현실이 갈리는 구성**으로 보인다. 의존명사·보조용언이
  그렇다. ~~붙여 발음한 오디오에 정서법대로 `갈 수 있어요`를 넣자 PronScore가~~
  ~~100 → 48로 떨어졌다(정확도 82 / 완성도 67 / 유창성 31).~~
  ← REST 실측 94.0 → 87.4. **방향은 유지되나 크기는 한 자릿수다.**
  8.5의 "띄어쓰기를 세트에 고정한다"와 4.3의 "어절 분할을 고정한 채로만 가설을
  세운다"는 그대로 옳다

**확정 5 보강 — 완성도를 지배하는 것은 ErrorType 발생 여부다 (2026-08-10 실측)**

어절 1/2/3/4/5/7개 문장 6개를 쟀더니 CompletenessScore가 전부 100,
PronScore는 95.2~100이었다(8.9 B군). 오늘 잰 10문장 전부 ErrorType None이다.

확정 5의 식(오류 없는 어절 수 ÷ 참조 어절 수)과 "어절이 적을수록 1건 실패의 타격이
크다"는 그대로 유지된다. 다만 **분자를 정하는 것은 어절 수가 아니라 ErrorType 발생
여부다.** 오류가 하나도 없으면 어절이 1개든 7개든 100이다.
어절 수는 오류가 났을 때 타격 폭을 정할 뿐이다.

### 8.9 2026-08-10 실측 — 18문장 40회 (화자 1인)

조건: 화자 1인, 폰 녹음 m4a 18문장, Speech Studio 40회. 전부 JSON 값이다.
**표본이 화자 1인·문장 18개이므로 아래 어느 항목도 일반화하지 않는다.**

**A군 — 원어민 눈금 재측정** (3.2에 반영)

| 문장 | 인수인계 기록(화면) | PronScore | AccuracyScore |
|---|---|---|---|
| 감사합니다 | 100 | 100 | 100 |
| 날씨가 많이 더워요 | 98 | 97.6 | 96 |
| 많이 드세요 | 95 | 94 | 90 |
| 만나서 반갑습니다 | 91 | 89.2 | 82 |

어제 기록은 화면 반올림 값이었다. JSON 기준으로 **4개 중 1개가 92 미달**이다.
미달한 01번만 SNR 14.66이고 나머지는 21~33이라 녹음 환경이 달랐다. 재녹음 후 재측정한다.

**B군 — 어절 수 사다리** (8.8 확정 5 보강)

어절 1/2/3/4/5/7개 문장 6개. **CompletenessScore 전부 100, PronScore 95.2~100.**
오늘 잰 10문장 전부 ErrorType None이었다.
→ 완성도를 움직이는 것은 어절 수가 아니라 ErrorType 발생 여부다.

**B군 부수 발견 — 하위 층 점수가 상위 값의 복사본으로 온다** (8.6 · 14절에 반영)

| 어절 | 어절 점수 | 음절 | 음소 |
|---|---|---|---|
| 조금 | 60 | 60, 60 | 5개 전부 60 |
| 시장에서 | 98 | 전부 98 | 전부 98 |
| 과일을 | 96 | 전부 96 | 전부 96 |
| 날씨가 (02번) | 98 | 89, 100, 100 | — |

한 응답 안에서 층이 복사되는 어절과 층마다 다른 어절이 섞인다. **조건 미확정.**

**C군 — 띄어쓰기, 8.8의 방향성이 뒤집힌다**

| 문장 | 붙임 | 띄움 |
|---|---|---|
| 한국 사람입니다 | 95.2 | 92.8 |
| 공부하고 있어요 | 94.6 | 85.0 |
| 갈 수 있어요 | ~~100~~ | ~~48.0~~ |
| 먹어 본 적 없어요 | 94.0 | 94.6 |

4개 중 3개에서 붙임이 높다. 8.8 A/B(띄움 97.6 > 붙임 66)와 반대다.

~~13번이 극단 사례 — 붙여 발음한 오디오에 정서법대로 `갈 수 있어요`를 넣자~~
~~PronScore 100 → 48(정확도 82 / 완성도 67 / 유창성 31).~~

← **정정 (2026-08-10). 그 숫자는 JSON이 아니라 화면 값이다.** 그 회차만 JSON을
받지 못하고 캡처만 남았는데 화면 숫자를 JSON처럼 다뤘다. 유창성 31이 그
표시였다 — 3.2가 기록한 함정(화면 32 / JSON 100)의 재현이다.

**52점 격차는 근거가 없다.** 같은 오디오를 REST로 다시 재면 **94.0 → 87.4,
gap +6.6**이다(아래 REST 재측정).

정서법과 발음 현실이 갈리는 구성(의존명사·보조용언)이 위험 지점이라는 판단은
**폐기하지 않는다. 방향은 유지하고 크기를 낮춘다 — 52점이 아니라 한 자릿수다.**
붙임이 유리한 사례가 실재한다는 것도 REST에서 그대로다(c12 +9.6 / c13 +6.6).

**D군 — 오류 가설 재채점이 Azure에서 작동하지 않는다** (4.3에 반영)

의도적으로 틀리게 읽은 녹음에 정상 참조와 오류 참조를 각각 넣었다.

| 녹음(실제 발음) | 정상 참조 | 오류 참조 | 차 |
|---|---|---|---|
| [반가씀니다] | 반갑습니다 98.2 | 반가씀니다 97.0 | -1.2 |
| [올마예요] | 얼마예요 95.2 | 올마예요 92.2 | -3.0 |
| [사요] | 싸요 89.8 | 사요 91.6 | +1.8 |
| [조은네요] | 좋네요 91.6 | 조으네요 94.0 | +2.4 |

가설이 맞을 때 점수가 튀어야 하는데 **최대 3.0점이고 부호도 갈린다.**

더 무거운 사실이 하나 있다. **틀리게 읽은 [반가씀니다]가 정상 참조로 98.2점이다.**
어제 정상적으로 발음한 04번의 같은 어절 `반갑습니다`는 69점이었다.
틀리게 읽은 쪽이 30점 가까이 높다. 다만 두 값은 다른 회차·다른 녹음이라
녹음 조건 차이가 섞여 있다. **이 비교는 통제되지 않았다.**

8.7의 NBestPhonemes 2순위 신호는 17번에서 91 / 69로 높게 나왔다. 신호는 있다.
그러나 `Phoneme` 문자열이 비어 있어 무엇과 혼동했는지 알 수 없고, 그래서 이 회차에서는
활용하지 못했다. 8.7의 "어디를 좁히는 데만 쓰고 무엇을 정하는 데는 못 쓴다"와 일치한다.

**확정**

1. 인수인계 문서의 원어민 눈금 4개는 화면 값이었다. JSON 값은 100 / 97.6 / 94 / 89.2다
2. CompletenessScore는 ErrorType이 하나도 없으면 어절 수와 무관하게 100이다
3. 참조 띄어쓰기의 효과는 방향이 고정돼 있지 않다. 붙임이 유리한 사례가 3건 나왔다
4. 오류 가설 재채점의 gap이 Azure ko-KR에서 최대 3.0점이고 부호가 갈린다
5. 한 응답 안에서 하위 층 점수가 상위 값의 복사본으로 오는 어절이 존재한다

**미확정 — 추측하지 않는다**

- 층 복사가 일어나는 조건
- 끊어 읽기 일치가 실제 변수인지. 끊어 읽기를 따로 측정하지 않았다
- 01번의 92 미달이 눈금 문제인지 SNR 문제인지
- gap이 작은 것이 프로브 세트 설계 탓인지 엔진 탓인지 (8.5 · 8.4)
- D군 결과가 다른 엔진에서도 같은지. Azure ko-KR 한 곳만 쟀다

**오늘 쓰지 않는 결론**

4절 오류 가설 재채점의 폐기, 엔진 교체, 음절 타일 폐기 — 셋 다 오늘 넣지 않는다.
표본이 화자 1인이고 세 결정 모두 되돌리는 비용이 크다.
오늘 문서에 들어가는 것은 측정 결과까지다.

**REST 재측정 — 하네스로 다시 쟀다 (2026-08-10, 34회)**

위 A~D군은 Speech Studio에서 나온 값이다. 프로브 하네스의 요청 규격이
확정된 뒤(PROBE.md 1-b) 같은 녹음을 REST로 다시 쟀다. 화자 1인, 34회.

**D군 재측정** — gap = 정상 참조 − 오류 참조

| 그룹 | 정상 참조 | 오류 참조 | REST gap | Studio gap |
|---|---|---|---|---|
| 15 반갑습니다 | 97.6 | 97.6 | **0** | +1.2 |
| 16 얼마예요 | 95.2 | 92.8 | +2.4 | +3.0 |
| 17 싸요 | 88.0 | 90.4 | −2.4 | −1.8 |
| 18 좋네요 | 91.6 | 94.0 | −2.4 | −2.4 |

**gap 최대 2.4점, 부호 제각각.** D군 결론이 REST에서 그대로 재현됐다.
4.3의 "전제 3번은 Azure ko-KR에서 성립하지 않는다"에 근거가 하나 더 붙는다.

**C군 재측정** — gap = 붙임 − 띄움

| 그룹 | 붙임 | 띄움 | REST gap | Studio gap |
|---|---|---|---|---|
| 11 한국 사람입니다 | 94.0 | 94.6 | −0.6 | +2.4 |
| 12 공부하고 있어요 | 94.0 | 84.4 | **+9.6** | +9.6 |
| 13 갈 수 있어요 | 94.0 | 87.4 | **+6.6** | ~~+52~~ |
| 14 먹어 본 적 없어요 | 94.6 | 95.2 | −0.6 | −0.6 |

**확정 3은 유지되고 숫자만 바뀐다** — 붙임이 유리한 사례가 Studio 3건, REST 2건이다.
방향이 고정돼 있지 않다는 결론은 같다. 폭은 최대 9.6점이다.

34회 전부 `CompletenessScore` 100, `ErrorType` None이었다.

### 8.10 EnableMiscue — 강제 정렬 스위치 (2026-08-10 실측)

Microsoft Learn FAQ: **"단일 샷 모드에서 EnableMiscue가 false로 설정되면
시스템은 인식된 텍스트가 참조 텍스트와 강제적으로 맞추게 합니다."**

Speech Studio UI에는 이 설정이 없다(고급 옵션은 Prosody 하나뿐). SDK/REST로만 된다.
**8.8과 8.9의 Studio 측정은 전부 기본값 false, 즉 강제 정렬 상태에서 나온 값이다.**

**작동한다 — 판별 2회 (2026-08-10)**

발화에 없는 어절을 참조에 하나 더 넣고 두 값으로 불렀다.
오디오 `13 갈 수 있어요.m4a`, 참조 `갈 수 있어요 정말`.

| | false | true |
|---|---|---|
| `Lexical` | **`갈 수 있어요 정말`** | **`갈 수 있어요`** |
| `정말` 어절 | 28점 / **Mispronunciation** | 0점 / **Omission** |
| PronScore | 73.6 | 71.0 |
| AccuracyScore | 68 | 60 |
| FluencyScore | 89 | 100 |
| CompletenessScore | 75 | 75 |

**`Lexical` 필드가 증거다.** false에서는 말하지 않은 `정말`이 인식 결과에 들어가
있고, true에서는 실제로 말한 대로만 나온다. FAQ의 서술이 그대로 관측된다.

완성도는 두 값 모두 75(4어절 중 3건 정상)다. false에서도 `정말`이 오류로 잡혀
분자에서 빠지므로 8.8 확정 5의 식은 두 모드에서 다 성립한다.

**어절 수가 참조와 일치하면 표면 점수에 차이가 없다**

D군·C군 32회를 false/true 두 벌로 돌렸다. PronScore·Accuracy·Fluency·
Completeness·Words·ErrorType **32건 전부 동일하다.**

miscue는 삽입·누락을 잡는 설정이므로 참조와 발화의 어절이 일대일로 맞으면
잡을 것이 없다. 위 판별 2회가 그 해석을 뒷받침한다.

**하위 층은 미세하게 움직인다.** 17쌍 중 16쌍에서 차이가 있었다.

- `Confidence` — 16쌍 전부, 소수점 4~5자리
- 음소·음절 `AccuracyScore` — 1~3점. 어절 층까지 올라온 것은 1건뿐이다
  (`공부하고` 86 → 84)
- `Offset`·`Duration` — 0.01초 단위
- `NBestPhonemes` 2순위 — 크게 흔들린다 (66 → 41, 37 → 22, 20 → 1)

**폐기된 추정 — 되살리지 않는다**

- ~~우리가 보낸 `EnableMiscue` 키가 채점기에 닿지 않는다~~
  ← 판별 2회로 반증. 키는 닿고 응답이 갈린다. 32회에서 표면 점수가 같았던 것은
  참조와 발화의 어절이 전부 일치해 잡을 것이 없었기 때문이다

**하네스는 이 값을 필수 인자로 받는다.** 기본값이 없다. 어댑터에 `true`가 박혀
있었고 Studio 측정은 전부 false였다 — 그대로 돌렸다면 두 값이 다른 것을 모른 채
비교했을 것이다. 보낸 값은 결과 JSON과 리포트 머리에 함께 남는다. PROBE.md 4단계.

표본은 화자 1인·문장 9개·호출 34회다.

### 8.11 AI-Hub 데이터 (2026-08-11 실사)

#### 8.11.1 데이터 개요

「교육용 영어 모국어 사용자의 한국어 음성 데이터」

| 항목 | 값 |
|---|---|
| 용량 | 압축 208.72 GB / 해제 314.68 GB |
| 오디오 | wav 525,285개, **927.0시간** |
| 라벨 | JSON 525,285개 (1:1), TextGrid 390,656개 |
| 포맷 | PCM 16-bit 모노 100%. 48k 80% / 44.1k 17% / 16k 1.3% |
| 화자 메타 | proficiency 4단계, TOPIK 등급·점수, 모국어(전량 영어), 성별, 출생연도 |

**길이 수치 정정** — 최초 실사의 517.1시간은 오류다. wav `fmt` 청크의
`nAvgBytesPerSec`가 파일의 95%에서 2배로 잘못 적혀 있었고 그 필드로 나눴다.
전수 재측정 927.0시간이 라벨 `RecordedTime`과 일치한다(최대 0.005초 차).

**과제코드 (추정 — 공식 정의 미확인, 구축활용가이드가 다운로드에 없음)**

| 코드 | 발화 중앙값 | 태그율 | 추정 |
|---|---|---|---|
| RW | 0.87초 | 19.25% | 단어 |
| RS | 3.36초 | 32.73% | 문장 |
| RP | 40.24초 | 59.18% | 문단 |

**우리가 쓰는 것은 RS다.** 문장 단위 채점이므로.

#### 8.11.2 오류 태깅

pronunciation 계열 390,656건 중 **86,724건(22.2%)에 태그, 총 159,254개.**
Speech 계열에는 0건. 상위 유형: 초성 22.8% / 모음 18.8% / 종성 11.1% /
경음화 8.4% / 비음화 7.1%. 태그 위치는 start/end 초 단위.

주의 — 86,724는 RW·RP·운율 태그를 모두 포함한 수다.
**RS + 1~10초 + 음소 단위 태그 조건의 실제 상한은 23,130건**이다 (08-12 확인).

#### 8.11.3 억양 — 이 데이터로는 검증할 수 없다 (확정)

**문형(의문/평서) 라벨이 없다.** 대안으로 경계억양 오류 681건을 정답지 삼아
문말 320ms F0 기울기를 쟀으나(praat-parselmouth) 정확도 0.53~0.56,
기준선 0.5. **신호가 없다.**

원인이 갈리지 않는다 — 경계억양 태그 1,720개 중 문말 320ms 안에서
시작하는 것은 **29.7%뿐**이다. 한국어 경계억양은 억양구 경계마다 붙고
태거는 그것을 찍었다. 우리 기능은 문말만 본다. **서로 다른 것을 가리킨다.**

**확정 — 억양 검증은 자체 녹음으로 간다.** 규모·설계는 착수 시점에 정한다.
기능 자체(pitch.js)는 구현되어 작동 중이며 검증만 남은 상태다.

#### 8.11.4 표기 체계 — 기준과 도구가 다른 단위로 자른다

| | 단위 | 종수 |
|---|---|---|
| 기준 TextGrid | 한글 호환 자모 | 40 |
| korean_mfa 음향모델 | IPA | 104 |

```
좋다   기준 ㅈ ㅗ ㅌ ㅏ (4)     MFA tɕ o t̚ t ɐ (5)
```

둘 다 틀리지 않았다. **분절 규약이 다르다.** 병합 없는 안전한 1:1은
`ㅇ↔ŋ` 1종뿐이었다. `korean_jamo_mfa`는 이 문제를 풀지 않는다 —
사전의 표기 키만 자모이고 발음은 IPA다. **자모 단위 음향모델은 존재하지 않는다.**

#### 8.11.5 대응표 — 3.4의 결정으로 닫혔다

3.4 적용 + 사전 전수 집계로 **확정 29단위, MFA 음소 104종 전부 배정.**
산출물 `out/aihub_exp/phone_map.csv`. 잔여 21항목은 사람이 답했다.

**받침을 위치로 복원할 수 있다** — 기호로는 초·종성이 같은 `ㅂ`이지만
"자음 뒤에 모음이 오면 초성, 아니면 종성 / ㅇ은 항상 종성"으로 복원된다.
표본 200건 5,264구간에서 위반 0건(라벨 이상치 제외). **3.4의 ⑤가 실행 가능하다.**

**대응표의 용도는 화면(타일 표시)이다.** 검증·학습에서는 쓰지 않는다 —
태그가 초 단위이므로 시간 겹침으로 맞추면 되고, 자모로 묶는 과정 자체가
파일의 63.4%에서 개수 불일치를 만들었다(08-12 원인 분해: 분절 단위 차이 65%).

#### 8.11.6 데이터 취급 경계 (확정)

로컬에서는 자유롭게 다룬다. 막는 것은 외부로 나가는 것뿐이다.
- 공개 저장소에 커밋하지 않는다
- 원본 오디오·라벨을 외부 API(Azure 등)로 보내지 않는다
- 출시물·공개 문서에 원본 발췌를 싣지 않는다
- 집계 수치·상관계수 등 파생 결과는 나가도 된다

약관 — 1항 출처 표기(한국지능정보사회진흥원), 4항 학습용 한정,
7항 상업적 이용 별도 협의. **7항은 이 경로가 제품에 들어가는 시점에 문의한다.**

### 8.12 자체 음절 채점 검증 — GOP에서 학습 판별기까지 (2026-08-11~12)

#### 8.12.1 요약

핵심 질문은 하나였다 — **"몇 번째 소리가 틀렸는지" 기계가 짚을 수 있는가.**
음절 타일의 존폐가 여기 걸려 있다(0절 관문 b의 자체 구현 경로).

| 단계 | 위치 적중 top-1 | 무작위 대비 |
|---|---|---|
| GOP 단독 | 15% | 1.8× |
| + 학습 (2만 건) | 30% | 3.9× |
| + 학습 (전량 4.6만 건) | **34.1% ± 0.6** | **3.9×** |

top-3는 61.6%. 시드 5개 평균이며 출렁임 ±0.7%p.

**결론 — GOP 단독은 부족하고(확정), 정답지 학습이 성능을 두 배로 올렸으며,
데이터 규모의 수확은 체감 구간에 들어섰다.** 다음 카드는 데이터가 아니라
음향 표현(자기지도학습 모델)이다.

#### 8.12.2 GOP 계산은 가능하다 (실증)

kalpy 0.10.4. 분모는 프레임별 전 pdf 음향 로그우도의 최댓값.
공식 (분자−분모)÷프레임 수. 분자가 정렬기 자신의 per_frame_likelihoods와
최대 절대차 0.000004로 일치. 600건 41분(파일당 4.1초, 96.7%가 우도 행렬).

#### 8.12.3 GOP 단독 측정과 세 가지 흠

1차: top-1 20.8%(무작위 13.6%), AUC 0.605, 분포 겹침 0.82.
흠 셋을 고쳐 재측정 —
- **정렬 실패 9.5%** → beam 상향(100/200)으로 57건 중 56건 해결. 적중률 변화 없음
- **fMLLR 화자 적응** → 켰으나 화자 83.9%가 발화 1건이라 440/503명이
  항등행렬. **설정 실수가 아니라 표본 구조의 문제였다.** top-1 24.1%로 소폭 상승
- **자모 묶기 63.4% 불일치** → 원인 분해: 분절 단위 차이 65% / 대응표 13% /
  데이터 이상 1% / 미분류 21%. **해법은 대응표 수정이 아니라 자모로 묶지 않는 것.**
  태그가 초 단위이므로 시간 겹침으로 라벨링한다 (8.12.4부터 적용)

#### 8.12.4 정답지 학습 — 성능이 두 배가 됐다

특징: GOP(SA), 음소, 길이, 상대 위치, 앞뒤 음소, 사후확률 상위 3,
forced_rank, 프레임 GOP 최솟값·표준편차, 발화 평균 GOP, proficiency.
라벨: 태그 구간과의 시간 겹침. 화자 단위 70/30 분할.

| 학습량 | LightGBM top-1 (고정 시험지) |
|---|---|
| 3.6만 행 | 0.208 |
| 36만 행 | 0.300 |
| 75만 행 | 0.309 |

**규모 곡선이 꺾였다** — 10배가 +9.2점, 그다음 2배가 +0.9점.
로그 기울기 0.091 → 0.028. 로지스틱 회귀는 규모에 거의 반응하지 않으며(0.24 부근),
**작은 데이터에서는 로지스틱이, 큰 데이터에서는 LightGBM이 이긴다.**
이 역전은 08-12 검증에서 실제 현상으로 확인됐다(시험지 고정, 시드 5개,
집계 코드 결함 없음).

특징 기여(어블레이션): 음소 정보가 최대(−7.8점), 길이·위치, GOP 계열 순.
사후확률과 화자 정보는 기여 없음.

#### 8.12.5 유형별·등급별 — 잘 잡는 것과 못 잡는 것

| 잘 잡는다 (AUC) | 못 잡는다 (AUC) |
|---|---|
| 구개음화 0.851 | 연음규칙 0.668 |
| 경음화 0.813 | ㄴ-첨가 0.550 |
| 초성 0.787 / 종성 0.709 | |

오답 분석 — 거짓 양성 15건 **전부 ㅃ(p͈)**, 13건이 b로 오인:
음향모델의 알려진 성질이 전량에서 재현됐다. 거짓 음성은 9/15건이 모음 ɐ:
음운 규칙 태그 구간이 앞뒤 모음을 덮어 라벨이 번진 것(시간 겹침 50% 기준의 부작용).

**눈금 (부산물, 채점 방식과 무관하게 유효)** — 발화 점수가
Beginner 0.403 → Intermediate 0.310 → Advance 0.271 → Fluent 0.227로
단조 감소. 등급 순서를 재현한다. 단 사분위가 크게 겹쳐 개인 판별용은 아니다.

#### 8.12.6 판단 — 2026-08-12 확정

- **GOP 단독 폐기.** 규모를 키워도 15%에서 움직이지 않았다
- **정답지 학습 경로 채택.** "포기하지 않고 데이터에서 최대한 뽑는다"
  (사람 결정, 08-11). 34%까지 왔고 top-3 62%는
  "의심 구간 후보 표시" 수준의 제품 기능을 시야에 넣는다
- **34%로 단정 표시("이 글자가 틀렸다")는 만들지 않는다.**
  10절 "부정확한 점수는 없는 점수보다 나쁘다" 적용
- **다음 카드는 자기지도학습 음향 표현이다** (wav2vec2 계열).
  규모 곡선이 꺾였으므로 데이터 추가가 아니라 표현을 바꾼다.
  이 검증 전에는 음절 타일 관문(0절 b)을 닫지 않는다
- 대안 유지 — 어절 타일(Azure)은 여전히 확인된 안전판이다

**2026-08-13 갱신 — 자기지도학습 검증 완료. 8.13 참조.**
"다음 카드는 자기지도학습 음향 표현" 판단이 실행되어 top-1 44.1%로 확인됐다.

→ 15.3 참조

#### 8.12.7 사용자 데이터 축적 — 설계 방향 (2026-08-12, 사람 결정)

기준은 영어권(AI-Hub)으로 잡고, **다른 언어권 사용자가 앱을 쓰면
그 데이터를 모아 반영·업그레이드한다.** 데이터가 쌓일수록 판별기가
좋아지는 선순환이 이 경로의 장기 자산이다.

전제 둘 —
- **학습 활용은 별도 동의** (전략서 9장 방침 유지). 동의한 사용자 것만 자산이 된다
- **첫 버전부터 로그를 남긴다** (전략서 5.4). 녹음만이 아니라 맥락
  (문장, 모국어, 점수, 재시도)이 함께 남아야 쓸 수 있다

구체 설계는 앱 개발 착수 시점에 한다.

#### 8.12.8 폐기된 지표 — 되살리지 않는다

| 폐기 | 왜 |
|---|---|
| ~~AI-Hub 총 517.1시간~~ | wav 헤더 오류로 나눈 값. 실측 927.0시간 |
| ~~음소 개수 불일치 44.0% / 58.8% / 84.0%~~ | 정렬 품질이 아니라 표기 체계(자모 40 vs IPA 104) 차이의 산물 |
| ~~음소 경계 중앙값 10ms~~ | 1:1 제약 없는 최근접 거리. 가설이 조밀할수록 작아지는 값 |
| ~~(a)(b) 최대 정확도 0.9458~~ | 다수 클래스 기준선과 동일. AUC·균형정확도로 읽는다 |
| ~~관문 A (MFA 정렬 vs 사람 정렬 대조)~~ | 검증 단계에서는 TextGrid 사람 경계를 쓰면 되므로 선행조건이 아니었다 |
| ~~"태그 발화 86,724건으로 학습"~~ | RS+음소 태그 조건의 실제 상한은 23,130건 |

### 8.13 신형 음향 특징 — top-1 44.1% (2026-08-12~13)

#### 8.13.1 요약

8.12.6의 판단(음향 표현 교체)을 실행했다. **위치 top-1이 34.1% → 44.1%가 됐다.**

| | top-1 (프레임5+) | top-3 (전체) | AUC | 무작위 대비 |
|---|---|---|---|---|
| 기존 (MFA GOP 특징) | 0.3412 ± 0.0056 | 0.5710 | 0.7630 | 3.86× |
| **신형 (+ 모델 ① 특징)** | **0.4405 ± 0.0045** | **0.6720** | **0.8237** | **4.98×** |

시드 5개 평균. 표준편차가 오히려 줄었다(안정적).
데이터·파이프라인·하이퍼파라미터는 8.12와 동일, 특징만 바꿨다.

#### 8.13.2 후보 조사와 선정

HuggingFace에서 wav2vec2/HuBERT 계열, 한국어 포함, 라이선스 확인으로 5개를 추렸다.

| 후보 | 크기 | 성격 | 결과 |
|---|---|---|---|
| ① slplab wav2vec2-xls-r-300m 음소 CTC (한국어 낭독 108h) | 315M | 음소 받아쓰기 | **채택** |
| ② slplab (외국인 발화 학습판) | 315M | 음소 받아쓰기 | **사용 금지 — 아래** |
| ③ team-lucid hubert-base-korean (한국어 4,000h) | 94M | 표현 | 탈락 |
| ④ Kkonjeong wav2vec2-base | 94M | 자모 CTC | 라이선스 미표기로 제외 |
| ⑤ facebook xls-r-300m (128개 언어) | 300M | 표현 | 목록만 |

**② 사용 금지 (확정)** — 우리 검증 데이터(AI-Hub 동일 데이터셋)로 학습된 모델이다.
검증 성적이 좋아도 실력인지 암기(누수)인지 구분할 수 없다. 라이선스도 미표기.
이후 세션에서 "우리 데이터로 학습된 모델이 있다"며 다시 올라오면 이 항목을 근거로 막는다.

#### 8.13.3 5,000건 비교 — 판정 기준을 미리 정하고 돌렸다

진입 기준(top-1 +3.0%p 이상, 차이 > 시드 sd 합)을 사전에 정했다.

| 조합 | top-1 | A 대비 |
|---|---|---|
| A 기존 특징만 | 0.2651 ± 0.0098 | — |
| B 기존 + ③ 표현 | 0.2543 | −1.1%p |
| **C 기존 + ① 표현 + CTC 점수** | **0.3518 ± 0.0220** | **+8.7%p** |
| D ③ 표현만 | 0.2112 | −5.4%p |

**교훈 — "한국어를 많이 들은 모델"(③ 4,000h)이 아니라
"음소를 받아 적도록 훈련된 모델"(① 108h)이 이겼다.**
과제 정합성이 학습량보다 중요했다. B·D가 A보다 낮았다는 것은
표현 벡터를 그냥 더하는 것은 도움이 안 되고, 음소 확률(CTC)이 실질 기여라는 뜻이다.

#### 8.13.4 모델 ①의 성질 (실측)

- vocab 40단위 음소. **평음/경음/격음이 갈려 있고 받침 미파음(k̚ p̚ t̚)이
  별도 단위다 — 3.4의 ④⑤ 결정이 요구하는 구분이 출력에 내장돼 있다**
- MFA 음소 → vocab 매핑 96.1% 성공. 제외 3.9%는 전부 활음(j·w·ɰ·ɥ) —
  vocab이 활음+모음을 한 단위로 묶기 때문. 오매핑 0
- CTC가 첨예해 프레임 대부분이 블랭크(중앙 0.83). "기대 음소 평균 확률"은
  구조적으로 작아 MFA GOP 분모와 성격이 다르다. 다만 블랭크 제외 시
  기대 음소가 대개 1등(3위 내 67.3%)이라 신호는 살아 있다
- 프레임 간격 20ms (MFA 10ms의 2배). 환산은 나눗셈 하나. 단
  "프레임<5" 기준은 신형 프레임 기준으로 재정의 필요
- 전 오류 유형이 올랐다. 최대 개선은 기존 최약점이던 **ㄴ-첨가 0.550 → 0.762.**
  유형 간 격차 0.30 → 0.19로 축소

#### 8.13.5 비용 실측 — 서버 필요는 확정, GPU가 판을 바꿨다

| | 노트북 CPU | 노트북 GPU (RTX 3060, fp16 batch 4) |
|---|---|---|
| 처리량 | 0.78건/s | **28.45건/s (36.5배)** |
| 전량 40,817건 | 14시간 17분 | **약 24분** |

**315M 모델은 사용자 폰에서 돌 수 없다. 이 경로가 제품에 들어가면 서버가 필요하다.**
GPU 실측 상세 (2026-08-13, out/gpu_check/GPU.md):
- 모델 로드 fp32 2.0 GB / fp16 1.7 GB — 6 GB에 여유 있게 들어간다
- 최적점은 fp16 batch 4. batch 16은 GPU 메모리 한계(5.9/6.1 GB)로 급락
- **fp16의 출력 오차(최대 절대차 0.27)가 판별 성능에 미치는 영향은 미확인.**
  fp16을 채택하려면 같은 표본에서 top-1 44.1% 유지 확인이 선행이다
- 미세조정 1스텝이 6 GB 안에서 돈다 — AMP fp16 기준 2.6 GB 여유.
  **미세조정 카드가 이 노트북에서 실행 가능하다** (1스텝 확인만, 실행은 안 함)

램 7.6GB 환경에서 병합·학습이 터져 memmap 구조로 바꿔 통과했다(방법 불변).
메모리 증설(+16GB) 후에는 해당 없는 제약이다.

**환경 변경 (2026-08-13)** — WSL을 C에서 D로 이전했다(C 15 GB 회수,
이후 설치는 D를 씀). 배포판 이름이 Ubuntu → **Ubuntu-D**로 바뀌었다.
`wsl -d Ubuntu`를 박아 쓴 스크립트는 수정 필요. 기본 배포판으로 설정돼
`wsl`만 치면 된다. export 백업은 D:\wsl_backup\에 보관.

#### 8.13.6 판단 (2026-08-13)

- **신형 특징(모델 ① 조합 C) 채택.** 44.1%는 논의 초기에 눈높이로 삼았던
  '절반 근처'(top-1 50%)에 처음으로 닿는 거리다. 공식 관문 기준은 아니며,
  유효한 원칙은 8.12.6의 '단정 표시는 만들지 않는다'다.
  top-3 67.2%는 "의심 후보 표시" 기능을 실질 사정권에 넣는다
- **44%로도 단정 표시는 아직 만들지 않는다** (8.12.6의 원칙 유지)
- **다음 수 두 개, 순서 확정** — ① fp16 품질 확인(빠름, GPU 채택의 전제),
  ② 미세조정 1차(44%를 올릴 본명 카드, GPU 실측으로 실행 가능 확인됨).
  하이퍼파라미터 탐색·더 큰 모델은 미세조정 결과를 본 뒤에

→ 15.3 참조

#### 8.13.7 폐기·주의 (14절에도 반영)

- ~~"③ hubert-base-korean이 한국어 4,000h라 유력"~~ ← 실측으로 반증.
  더하면 내려간다(B: −1.1%p). 재검토 대상으로 올리지 않는다
- 신형 프레임은 20ms다. 기존 문서의 "프레임 5 미만" 기준을 신형 결과에
  그대로 적용하면 안 된다

---

## 9. 음성 자산 — 2026-07-30 확정

~~원어민 음성은 직접 녹음해 사전 캐시 (TTS 비용 0원)~~ ← **폐기**

**확정: 사람 목소리 직접 녹음은 하지 않는다. 유료 TTS로 생성한다.**

- 유료 플랜 출력물은 구독 종료 후에도 상업 이용권이 남는다 (무료 플랜 출력물은 상업 이용 불가)
- 문장이 쓰이는 대로 배치 생성하고 전량 직접 검수
- 문장 1000개 도달 시 외부 스토리지로 이전. 그 전까지는 저장소 내
- 오디오 base URL은 단일 상수로 관리

**부수 효과: 오류 샘플도 TTS로 만들 수 있다.**
표기를 표준발음형으로 비틀어 입력하면 원하는 오류 음성이 나온다.
예: 받침 탈락 샘플 → `반가씀니다`를 그대로 TTS에 입력

한계: TTS 오류는 **범주적**(음소 교체), 실제 학습자 오류는 **연속적**(ㅓ와 ㅗ 사이).
→ 선별 시험으로만 유효. 깨끗한 오류도 못 잡으면 탈락, 잡으면 "아마도 통과".
진짜 분포는 비공개 테스트 실사용 데이터에서 얻는다.

**TTS 벤더 선정 기준 (가중치 순)**
1. 의문문/평서문 억양 구분, 감정·속도 제어 — 8.1에 따라 억양 층의 채점 기준이 되므로 최우선
2. 음운 변동 처리 정확도 — 테스트: 많이[마니] / 반갑습니다[반갑씀니다] / 신라[실라] / 국물[궁물] / 값이[갑씨]
3. 상업 라이선스 영속성, 앱 내 재배포 허용 여부
4. 단가 (문장 1000개 기준 총액)
5. **표기를 비튼 입력을 교정하지 않고 쓰인 대로 읽는가** — 8.5의 오류 샘플 생성에 필수

조사 대상: ElevenLabs, Google Cloud TTS(ko-KR), Azure TTS(ko-KR), 네이버 CLOVA Voice, Typecast, Supertone

### 9.1 도구 확정 — 타입캐스트 (2026-08-18 청취 · 08-19 계약)

~~ElevenLabs 유력~~ ← **기각 (2026-08-18).**

*이 문서에 "ElevenLabs가 유력하다"고 적힌 문장은 없었다. 위 조사 목록의 첫머리와
저장소 밖 문서(전략서·프로젝트 지침)에 그 인상이 남아 있었을 뿐이다. 여기서 닫는다.*

근거 — 같은 10문장을 여섯 경로로 들었다.

| 후보 | 결과 |
|---|---|
| **타입캐스트** | **가장 자연스러웠다 → 채택** |
| Azure TTS ko-KR (SunHi·InJoon) | 감정 스타일 0종, 억양 평탄. "영 아니야" |
| Kokoro | **한국어 미지원.** kokoro 0.9.4의 `LANG_CODES`에 `ko`가 없다 (CC가 소스 확인) |
| 네이버 CLOVA Voice · 수퍼톤 | 웹 체험으로 비교 |
| ElevenLabs | 한국어에 외국인 억양이 있다는 웹 리뷰. **미청취** — 직접 듣고 내린 결론이 아니다 |

**확정: 타입캐스트 API 라이트 $15/월.** 구독은 **문장을 추가하는 달에만 켠다.**

**과금은 입력 글자당 1크레딧이다** (공백·문장부호 포함). 6회 연속으로 글자수와
정확히 일치했다.

~~과금은 오디오 길이 기준, 초당 약 3크레딧~~ ← **오독 정정 (2026-08-19).**
생성 직후 구독의 `used_credits`를 읽은 값(970)을 확정값으로 삼은 결과였다.
그 값은 아직 반영 중이었고 정착값은 2,016으로 글자수와 맞았다.
**카운터가 멈출 때까지 기다렸다 읽어야 한다** (실측 정착 40~62초).

**라이선스 (타입캐스트 고객센터 회신 2026-08-19)**
- 유료 기간 중 **생성·다운로드를 마친** 파일은 해지 후에도 계속 사용할 수 있다
- 재생성은 재구독해야 한다
- 무료 플랜 생성물은 **상업 이용 불가** (약관 2조 5항)

즉 구독을 끄기 전에 받아 두는 것이 계약 조건이다. 9.3의 보관 세 곳이 그래서 있다.

### 9.2 보이스 확정 — 우성(남) · 이현(여) (2026-08-19 청취)

| | voice_id |
|---|---|
| 남 **우성** | `tc_668f4f533ea5c6ce5e43fd48` |
| 여 **이현** | `tc_68ddea1e462b169ddd20b74d` |

후보 12개 → 3개(우성·강일·이현)로 좁혀 **50문장 전부**를 세 목소리로 듣고 골랐다.

- **우성 40/50 1등급** → 채택
- 강일 39/50으로 점수는 비슷했으나 **"얼마입니까"를 "얼맙니까"로 읽는다.**
  참조 텍스트와 발화가 어긋나면 8.1의 정합성이 깨진다 — 화면에 뜬 문장과
  들려주는 소리가 다른 것이고, 학습자가 따라 한 것이 참조와 다르게 채점된다.
  감정 과잉과 의문문을 평서로 읽는 사례도 있었다 → 탈락
- **이현 44/50** → 채택

**남/여를 사용자가 온보딩에서 고른다.** 따라 할 표본이므로 자기 음역대여야 한다.

강일은 Drama 컬렉션의 "실제 발화" 버전 후보로 **미결**로 남긴다.

### 9.3 손질 절차와 seed 비재현 (2026-08-19)

1. 1차 생성 (전 문장 × 보이스)
2. 청취 등급 — 1 / 2 / 3 / 불합격
3. **2·3등급 문장만** 재생성 3회(seed를 달리) + 텍스트 손질 1(부호·붙임) + 감정 프리셋 1
4. 사람이 듣고 고른다

16문장을 손질했고 **전부 원본이 아니라 변형이 채택됐다.**

**API의 seed는 재현되지 않는다 (2026-08-19 실측).** 같은 seed·같은 텍스트로 두 번
만든 파일이 서로 달랐다(바이트도 길이도). 요청에 `seed`가 실린 것은 확인했다.

→ **확정 mp3가 유일본이다.** 어떤 변형을 골랐는지 적어 두어도 그것으로 복원되지
않는다. 9.1의 라이선스 조건(구독 기간 안에 다운로드)과 겹쳐, 파일을 잃는 것이 곧
자산을 잃는 것이다. 그래서 보관을 세 곳에 둔다.

- 로컬 `D:\aihub_work\tts_typecast_final_20260819\` (원본 폴더)
- `Naruve Data\tts\` 의 zip (저장소 밖)
- R2 `naruve-ganada-audio` 의 `tts/v1/` 접두어 (사용자 음성과 같은 버킷, 접두어로 분리)

### 9.4 앱 배치 (2026-08-19)

`docs/audio/{m,f}/{hash}.mp3` **100개, 7.9 MB.** `m` = 우성, `f` = 이현.
파일명은 한국어 원문의 해시이고 `docs/js/audio.js`의 `audioName()`이 같은 값을 만든다.
같은 폴더의 `index.json`이 "이 문장은 파일이 있다"를 알린다.

서비스워커 프리캐시에는 넣지 않는다 — 첫 방문에 7.9 MB를 받게 하지 않는다.
재생할 때 받아 빌드가 올라도 지워지지 않는 캐시에 담는다.

**폴백은 남긴다.** 목록에 없거나 로드에 실패하면 기기 TTS로 떨어진다.
문장을 고치면 해시가 바뀌어 그 문장은 다시 "파일 없음" 상태가 되기 때문이다.

### 9.5 API 억양 손잡이의 한계 (2026-08-19 확인)

요청에 넣을 수 있는 것은 **pitch · tempo · 감정 프리셋 7종 · seed** 뿐이다.
**문말 억양과 휴지를 직접 지정하는 수단은 없다.** SSML도 없다.

실효가 있었던 것은 **텍스트 손질**(쉼표·느낌표·어절 붙임)과 **재생성**이다.
파라미터는 통과하지만 원하는 지점을 겨냥하지 못한다.

Timestamp TTS(단어 정렬)는 나중에 "낮은 단어만 잘라 듣기"를 만들 때 후보다.

---

## 10. 기술 (확정)

- 웹앱 + Capacitor 셸. Android 우선, iOS 2차
- 타겟 API 36
- 무료: 정밀 채점 총량 30회 → 이후 기본 모드
  (일일 할당 방식 기각 — 손익분기 전환율 14.3%로 비현실적)
- 원가 방어: 예시 음성 사전 캐시, 재시도 결과 캐시, 무음 절단, 발화 10초 상한
- 억양 층은 자체 pitch.js (Web Audio F0). Azure가 ko-KR 운율을 주지 않으므로 대체 불가

**온디바이스가 기각될 경우의 기본 모드 설계**
대안은 GPT-4o mini transcribe가 아니다(같은 LM 교정 문제). 대안은 **기본 모드에서 발음 채점을 하지 않는 것**이다.
- 기본 모드 = 억양(F0) + 발화 속도/길이 + 인식 통과 여부. 원가 0
- 정밀 모드 = 클라우드 음소 채점
- "무료는 리듬을 본다, 유료는 소리를 본다"로 경계가 선명해진다
- **부정확한 점수는 없는 점수보다 나쁘다**

---

## 11. 콘텐츠 (확정)

- Standard / Everyday **강제 페어링 폐기**. 각각 독립 컬렉션
  (억지 페어가 "김치찌개 하나 주십시오" 같은 비자연 문장을 낳았음)
- 컬렉션: Standard 30 / Everyday 30 / Drama 25 / Sounds 15 (%)
- Sounds는 Browse에서 숨기고 진단으로 배정
- K-드라마·K-팝 IP 사용 불가. **드라마풍 상황과 일반 회화 표현은 자유**
- 문장 데이터 필드: 격식 수준, 적절 대상, 부적절 시 대안, 용례

---

## 12. 작업 방식

- 중요한 변경은 **제안 → 승인 → 실행**
- 파일 수정은 부분 패치가 아니라 전체 파일 교체
- 평가는 솔직하게. 안심시키는 말보다 문제 지적 우선
- 근거 없는 수치 금지. 추정이면 추정이라 밝힌다
- **프롬프트는 이슈 논의가 끝난 뒤 요청 시에만 작성한다** (2026-08-03)
- CC 작업 → 결과 보고 → 지시 → 결과 보고 순으로 핑퐁. 작업 중에는 새 지시를 얹지 않는다
- egress 정책에 막히면 우회하지 말고 보고만 한다
- **docs/ 에는 앱에 실릴 것만 둔다.** docs/는 GitHub Pages 배포 루트라
  안에 있는 파일은 전부 공개 URL로 접근된다. 개발 문서·결정 기록·
  실측 결과는 저장소 루트에 둔다 (DECISIONS.md, CLAUDE.md, PROBE.md와 같은 층).
  (2026-08-03)
- **저장소가 public이라는 것을 항상 전제한다.** docs/ 밖에 두면
  Pages 배포는 막히지만 저장소 자체가 공개라 누구나 읽는다.
  개인정보·사업자 정보·가족이나 조직에 관한 판단은 저장소에 올리지 않고
  프로젝트 지식에만 둔다. 한 번 커밋하면 나중에 지워도
  커밋 히스토리에 남는다. (2026-08-06)
- **측정값은 화면이 아니라 응답 JSON에서 읽는다.** Speech Studio 화면과 JSON이
  다른 값을 보인 사례가 있다(3.2). 화면에서 읽은 숫자를 이 문서에 옮기지 않는다.
  (2026-08-09)
- **auto-merge에 병합 전 검사 추가** (2026-08-24, Arga 승인·Claude 위임 결정).
  검사: docs/index.html·docs/privacy 존재, docs/*.js 문법. 실패 시 병합 중단.
  기각: 사람 리뷰 요구(1인이라 형식화), 유료 브랜치 보호(auto-merge와 충돌).
  목적은 침해 방어가 아니라 실수 방어다 — 깨진 빌드가 naruve.app에 나가는 것을 막는다.

---

## 13. 착수 금지 (구조 확정 전)

- 문장 라이브러리 50→200 확장
- 인도네시아어 UI 51키 번역
- 오류 가설 사전 본격 구축
- 실제 TTS·Azure 호출 (키와 egress 준비 후 별도 지시)

이유: 화면 구조가 바뀌면 문구가 바뀌고, 채점 방식이 바뀌면 문장에 딸릴 메타데이터가 바뀐다. 먼저 하면 두 번 한다.

---

## 14. 알려진 이슈

- `~/.ssh/commit_signing_key.pub` 0바이트 → SSH 서명 로컬 검증 불가. 지금 고치지 않음
- egress 차단: Azure 엔드포인트, learn.microsoft.com, TTS API. 허용목록 추가 필요
- egress 차단: ETRI e-PreTX `epretx.etri.re.kr:8000` (http, 포트 8000).
  키는 발급받았으나 이 후보는 8.2에서 탈락했으므로 허용목록 우선순위는 낮다
  (2026-08-10)
- 전략서 v2의 "원어민 음성 직접 녹음"(5.1, 10장 3주차), "원어민 음성 비교"(4.2①) 항목이 현 결정과 불일치. 데이터 확보 후 일괄 개정
- **Azure ko-KR: `Syllables` 배열이 없는 어절이 있다** (2026-08-07 실측).
  "만나서"에서 배열이 통째로 없었다. 같은 응답의 나머지 6어절에는 정상적으로 왔고
  개수도 표기와 일치했다. **원인 추정** — 받침 ㄴ과 초성 ㄴ이 같은 소리라 음소가
  6개로 병합되어(정상 7개) 3-2-2 분할이 불가능해진 것으로 보인다. 같은 자음이
  연달아 만나는 경우에만 생기는 것으로 보이나 **표본 1건이라 확정이 아니다.**
  8.6의 폴백 규칙이 이 사례 때문에 있다. 재현 여부는 프로브의
  s1_ok / s1_batchim(둘 다 "만나서"로 시작)에서 확인된다.
  **추가 (2026-08-10 REST 실측)** — `좋네요`(ㅎ+ㄴ)에도 배열이 없었다.
  `만나서`(ㄴ+ㄴ)와 같은 모양이라 자음 병합 쪽으로 수렴한다.
  표본은 화자 1인의 두 문장이다
- ~~**Azure ko-KR: 어절이 1개면 `Syllables` 배열이 오지 않는다** (2026-08-09 실측).~~
  ~~참조 `저는한국사람입니다`(Words 1)에서 배열이 통째로 없었다. 위 "만나서" 사례와~~
  ~~**원인이 다르다** — 자음 병합이 아니라 어절 수 자체가 조건이다.~~
  ~~8.6의 폴백 규칙이 이 경우에도 걸린다. 표본 1건이다~~
  ← **반증 (2026-08-10).** `Words`가 1개인 REST 응답 16건 중 14건에 배열이
  정상으로 왔다. **어절 수는 조건이 아니다.** 8.8 확정 4 참조
- **Azure ko-KR: 하위 층 점수가 상위 값의 복사본으로 오는 어절이 있다** (2026-08-10 실측).
  `조금`이 어절 60 / 음절 60·60 / 음소 5개 전부 60으로 왔다. 같은 응답의
  `시장에서`(전부 98)·`과일을`(전부 96)도 같은 모양이고, 반면 `날씨가`는
  어절 98 / 음절 89·100·100으로 층마다 다르다. **조건 미확정이다.**
  표본은 화자 1인·문장 10개다. 8.6의 음절 타일 보류 근거 중 하나이며,
  복사되는 어절에서는 음절 타일이 새 정보를 주지 못한다
- 폐기 지표 6종을 8.12.8에 정리했다. 이전 기록에서 보이면 인용하지 않는다
- korean_jamo_mfa는 자모 단위 음향모델이 아니다(8.11.4). 재검토 대상으로 올리지 않는다
- AI-Hub 기준 TextGrid에 라벨 이상치 1.27%가 존재한다. 분석에서 제외했다
- 음향모델이 ㅃ을 b로 오인하는 성질이 있다(8.12.5). 자체 경로를 제품화하면
  ㅃ 관련 감점은 별도 보정이 필요하다
- fMLLR은 화자당 발화가 여러 건이어야 작동한다. 앱 사용자는 반복 사용으로
  자연히 충족되지만, 첫 발화에서는 화자 적응 없는 값임을 감안한다
- 모델 ②(slplab 외국인 발화 학습판)는 검증 데이터 누수 위험으로 사용 금지 (8.13.2)
- hubert-base-korean(③)은 학습량과 무관하게 이 과제에 기여하지 않음이 실측됨 (8.13.3)
- 신형 특징의 프레임은 20ms — "프레임<5" 기준 재정의 전에는 혼용 금지 (8.13.4)
- WSL 배포판 이름이 Ubuntu-D로 바뀜 — `wsl -d Ubuntu` 스크립트 수정 필요 (8.13.5)
- fp16 추출은 품질 확인 전까지 본실험에 쓰지 않는다 (8.13.5)

---

## 15. 음절 단위 표시의 하한선과 미세조정 판정 (2026-08-16 확정)

### 15.1 배경

미세조정 1차 실행(REPORT.md, 2026-08-15) 결과 A-2 top-1 0.4442.
같은 시험 세트의 A-1(미세조정 전 + LightGBM) 0.4173 대비 +2.69%p(전체), +1.94%p(프레임5+).
사전 기준(±2%p 무변화)을 전체 기준은 넘고 프레임5+ 기준은 못 넘어 판정이 갈렸다.
(2026-08-16 정정: 초안은 전체 기준 값에 프레임5+ 차이 +1.94%p를 잘못 붙였다.)
"넘었다/못 넘었다"를 정하기 전에 **무엇을 넘어야 하는지**가 문서에 없었다.
문서에는 8.12.6·8.13.6 "단정 표시는 만들지 않는다"와,
커밋 #35에서 기준이 아니라 초기 눈높이로 정정된 top-1 50%뿐이었다.
이 절이 그 빈자리를 채운다.

### 15.2 판정 기준선 — A-1

미세조정 효과는 **같은 시험 세트의 A-1 대비**로 잰다.
다른 실행의 44.05%와 비교한 +2.56%p는 참고값이며 판정에 쓰지 않는다.
근거: 시험 세트가 다르면 같은 대상을 잰 것이 아니다. 출처가 다른 값을 한 표에 섞지 않는다.

유의성은 시드가 아니라 **화자 단위 짝지은 부트스트랩**으로 본다.
고정 분할·bagging 없는 LightGBM은 시드를 바꿔도 결정적이라 시드 표준편차가 0이 나온다(실측).
그 0은 "잡음 없음"이 아니라 "이 방법으로는 잡음을 잴 수 없음"이다.

**주 지표는 top-1(전체)이다.** 프레임5+는 보조 지표로 함께 보고하되 판정에는 쓰지 않는다.
짧은 파일을 제외하면 분모가 줄어(2,274 → 1,905) 잡음이 커진다.
두 지표의 판정이 갈릴 때 주 지표를 따르고, 갈렸다는 사실을 기록한다.

**기준선은 2026-08-17 실행의 A-1(--parent ft)이다.** ft_lgb.json 기록값(0.4173)은 세 차례 재현 시도에서
복원되지 않았다(0.4178~0.4274 범위, 3회 시도 — sklearn 래퍼 포함, LOG.md). 파이프라인이 틀린 것이 아니라 설정 원본이 남지 않아
눈금이 0.5%p 안팎에서 흔들린다. 기록값은 참고로만 병기한다.
시드 표준편차 0의 원인 — 부모 Dataset을 한 번 binning하고 subset()으로 학습 행을 고르는 구조에서는
시드가 bagging·feature sampling에만 걸리고 둘 다 꺼져 있어 결정적이 된다. sklearn 래퍼는 random_state를
bin 표본 추출에까지 넘겨 이 성질을 깬다 (2026-08-17 실증).

### 15.3 화면 표시별 하한선

화면이 하는 판단은 두 가지이고, 재는 지표가 다르다.

| 표시 | 판단 | 지표 | 하한선 |
|---|---|---|---|
| 음절 타일 명암 | 약한가 아닌가 | 재현율 50% 이상 조건에서 정밀도 | **75%** |
| 이유 문구 ("받침을 닫으세요") | 어떤 오류인가 | top-1 (오류 태그 파일 기준) | **65%** |

재현율 조건이 붙는 이유 — 조건 없이는 자신 있는 하나만 칠하고 정밀도 100%를 주장할 수 있다.
놓치는 것보다 잘못 짚는 것이 더 나쁘다. 학습자는 안 짚은 것을 의심하지 않지만 잘못 짚은 것은 그대로 믿는다.

**이 수치에 공인된 업계 표준은 없다.** 틀렸을 때 학습자가 받는 피해 크기에서 역산한 판단이다.
실사용 로그가 쌓이면 "앱이 짚은 음절을 고쳤을 때 점수가 오르는가"로 직접 검증하고 재조정한다.
재조정하면 이 절에 날짜와 함께 남긴다.

### 15.4 시험 세트 정밀도는 실사용을 과소평가한다 (2026-08-16 추가)

시험 세트의 오류 태그 구간 비율은 **0.0296**이다(BOOTSTRAP.md, out/ 미커밋; 같은 값이 REPORT.md에도 있음).
기저율 3%에서 정밀도 75%는 정상 구간 오탐률 0.5% 이하를 요구하는 셈이라 AUC 0.84 모델이 내기 어렵다.
그러나 이 값은 원어민에 가까운 화자까지 섞인 데이터셋의 값이고,
앱 사용자(초급 학습자)의 오류 기저율은 이보다 훨씬 높다. 같은 모델이라도 기저율이 오르면 정밀도가 오른다.

따라서 **시험 세트에서 잰 정밀도는 참고 지표다.** 이 값 하나로 음절 표시의 생사를 정하지 않는다.
생사 결정은 실사용 데이터가 있어야 한다.
15.3의 75%는 "실사용 집단에서" 넘어야 할 값이며, 그 값을 지금 잴 수단이 없음을 명시한다.

### 15.5 이유 문구 — 첫 버전 제외 (2026-08-16 확정)

A-2 top-1 0.4442는 65%에서 20.6%p 아래다. 부트스트랩 구간을 뽑아도 부호가 바뀌지 않는다.
**이유 문구는 첫 버전에서 뺀다.** 명암만 남긴다.
클라우드 재학습(15.7)으로 메울 폭이 아니다.

(2026-08-17 확인) 재현 실행에서도 A-2 top-1 0.4411, 65%에서 20.9%p 아래. 결론 유지.

### 15.6 GBM 층 필수 (2026-08-16 확정)

B(헤드 단독) top-1 0.3676 vs A-2(헤드 + LightGBM) 0.4442. 7.7%p 차이.

~~신경망 헤드 단독으로 GBM 층을 대체~~ ← 기각 (2026-08-16)
  한 실행의 두 지표 모두에서 GBM이 7.7%p(전체)·8.5%p(프레임5+)를 만든다. 대체 안은 닫는다.

### 15.7 클라우드 GPU — 조건부 (2026-08-16 확정)

자체 모델은 출시 경로의 병목이 아니다. 핵심 화면(문장 점수, 단어 단위 타일, 예시 음성)은 Azure로 확정돼 있고,
자체 모델은 그 위의 정밀 층이다. 이 실험의 결과로 갈리는 것은 앱이 나오느냐가 아니라
첫 버전이 단어 단위냐 음절 단위냐이며, 단어 단위는 이미 확정된 대안이다.

- 클라우드는 **부트스트랩·정밀도 계산 결과가 "근접(정밀도 65~75%)"일 때만**, **5만 원 한도**로 재논의한다
- 넘음/미달이면 클라우드를 쓰지 않는다 (넘으면 불필요, 미달이면 5만 원으로 메울 폭이 아님)
- 전량 학습(추정 20만 원)은 출시 후 실사용 데이터가 있을 때로 미룬다.
  실사용 데이터가 AI-Hub(영어권)보다 대상 사용자(인니어권)에 맞고 더 싸다
- 실행 시 전제: 스팟 인스턴스 금지, 영구 볼륨에 체크포인트, 본 실행 전 소량 스모크로 재개 확인,
  AI-Hub 이용약관의 국외 반출 조항 확인

~~전량 데이터 + 5 epoch 수렴을 지금 클라우드에서 (추정 10~25만 원)~~ ← 기각 (2026-08-16)
  천장을 알기 위한 정보 구매인데, 그 정보는 출시 후 실사용 데이터로 더 싸고 더 정확하게 얻는다.

**2026-08-17 판정: 클라우드 쓰지 않음.** 재현율 50% 지점 정밀도 A-2 0.1336 → 미달. 근접선까지 4.9배.
5만 원은 물론 어떤 예산으로도 지금 메울 폭이 아니다. 조건부 재논의 항목은 닫는다.

### 15.8 미결

| 항목 | 막고 있는 것 | 해소 조건 |
|---|---|---|
| 실사용 기저율에서의 정밀도 | 음절 표시 재검토 | 출시 후 로그 (기저율 40% 이상이어야 75% 도달 — 추정) |

### 15.9 결과와 결정 (2026-08-17)

짝지은 부트스트랩(화자 290명, 1,000회): 주 지표 top-1(전체) 차이 +1.59%p, 95% 구간 [−0.05, +3.40] — 0과 +2%p 동시 포함.
AUC 차이 +0.0138 [+0.0096, +0.0181] — 0 미포함, 유일한 확정 개선.
해석: 미세조정은 구간 순위를 확실히 개선했으나 파일별 top-1 지목까지 안정적으로 이어지지 않았다.

**결정 셋:**
1. 미세조정 갈래는 '방향 유의, 크기 미확정'으로 닫는다. 추가 투자 없음
2. 클라우드 GPU 쓰지 않음 (15.7)
3. **첫 버전 화면은 단어 단위 타일로 확정.** 음절 명암·이유 문구 모두 제외. 실사용 로그 후 재검토

15.3의 하한선은 유지한다. 폐기가 아니라 현재 모델이 그 아래에 있다는 결론이다.
원자료: out/ft_exp/preds_A1_ft_seed0.csv, preds_A2_ft_seed0.csv, bootstrap_result.json, BOOTSTRAP.md (미커밋)

### 15.10 첫 버전 오류 설명 = 두루뭉실 층 (2026-08-18)

15.9의 단어 단위 확정과 문구 top-1 65% 미달 확정 위에서,
첫 버전은 **세 겹의 "두루뭉실" 설명을 무료·유료 공통으로** 준다.

| 겹 | 무엇 | 첫 버전에 되나 |
|---|---|---|
| (1) 어느 단어가 낮은지 | Azure 단어 점수 | **된다** |
| (2) 억양 방향 | F0. "끝이 올라갔어요 / 내려갔어요" | **된다** |
| (3) 방향 힌트 | 확정 문구가 아니라 "받침이나 모음이 흔들린 것 같아요" 수준 | 아직. 우리 모델(top-3 67%)이 GPU에 올라간 뒤 후보 |

근거 — 사용자가 "점수만 나오고 뭐가 틀렸는지 모르는" 상태를 피하는 것이
차별점이라는 판단이다. **확정 오류 설명(정밀도 65% 이상)은 L1별로 문턱을
넘는 순서대로 개방한다.** 전 언어를 한꺼번에 열지 않는다.

#### 15.10.1 억양은 총점에 들어간다 (2026-08-19 변경, 사용자 결정)

~~억양(위 표 (2)번 겹)은 방향 안내로만 쓰고 총점에는 섞지 않는다~~
← **변경 (2026-08-19).**

근거 — 의문↔평서 불일치는 **뜻이 바뀐다.** 3.1이 기준을 "원어민다움"에 두었는데
뜻이 바뀌는 오류가 점수에 전혀 반영되지 않으면 그 기준이 반쪽이 된다.
방향 안내만 뜨고 숫자가 그대로면 "억양은 점수가 아니다"로 읽힌다.

**확정 — 페널티 방식**

- 문장의 `t:` 태그가 `question`이면 **상승**, `statement`면 **하강**을 기대한다
- F0 판정이 **불일치**면 총점 **−15** (`INTONATION_PENALTY`, 실측 후 조정)
- 같은 −15를 **마지막 어절 타일**에 적용한다. 색이 그만큼 옅어진다
- 결과에 **"억양 −15"** 를 표시한다. 숫자가 왜 낮은지 모르면 채점이 임의로 보인다
- **일치 · 판정불가 · `exclam`은 감점 없음.** 판정 코드는 `question`이 아닌 것을
  전부 "하강 기대"로 싸잡는데 감탄문의 끝이 반드시 내려가야 하는 것은 아니다.
  판정은 보여주되 점수는 건드리지 않는다

~~가중 평균 (SCORE_MIX 30:70)~~ ← **폐기 (2026-08-19).**
두 가지 이유다. (1) 가중 평균은 발음 점수의 해석을 바꾼다 — "PronScore 85"가
더 이상 PronScore가 아니게 된다. (2) F0 판정은 거짓 음성이 있을 수 있는데,
평균이면 맞게 읽은 사람도 매번 손해를 본다. 페널티는 틀렸다고 판정된 회차에만
값을 치른다. CLAUDE.md의 보류 항목 **"통합 비율 재검토"는 이것으로 닫는다.**

**로그와 R2에는 원값을 남긴다.** 깎인 값은 화면에만 있고
`rawTotal`·`penalty`를 따로 기록한다(16.1). 원본을 고치면 캘리브레이션이
자기 꼬리를 물게 된다.

실기 2건에서 F0 판정이 정확했다 — "진짜?"를 평평하게, "괜찮아?"를 평평하게
읽은 회차를 둘 다 불일치로 잡았다 (2026-08-19).

## 16. 사용자 로그와 화면 단위 (2026-08-17 확정)

### 16.1 사용자 로그 저장 — 첫 버전부터 켠다 (2026-08-17 확정, 같은 날 보강)

**저장 단위는 녹음 파일이 아니라 이벤트다.**

```
이벤트 = { 익명ID(17.3), 시각, 종류, 문장ID, 세션ID, 페이로드 }
종류: 앱시작 / 온보딩완료 / 문장열람 / 예시재생 / 녹음 / 채점결과 / 건너뜀 / 크레딧소진 / 결제 / ...
녹음·채점결과의 페이로드: 음성 파일 참조(R2 키) + Azure 응답 전체(Words 배열·음소 점수 포함) + F0 결과
세션ID: 같은 문장을 연달아 시도한 묶음. 재시도 이력은 이 ID로 묶인다
```

음성은 R2, 이벤트는 KV 또는 경량 DB. 17절 구조 그대로.

**왜 이벤트인가 — 용도에서 역산했다.** 나중에 하고 싶은 것 넷이 요구하는 필드가 다르고,
녹음 파일만 저장하면 그중 셋이 안 된다.

| 용도 | 필요한 것 | 녹음 파일만으로 되나 |
|---|---|---|
| ① 음절 표시 재검토(15.9) — 실사용 데이터로 재학습 | 음성 + 답지. 답지 후보 중 "재시도 점수 변화"는 **세션ID로 묶여야** 가능 | 안 됨 — 개별 저장이면 재시도를 못 묶는다 |
| ② L1별 오류 사전 (핵심 자산) | 모국어 + Azure 음소 점수 + **학습 단계** | 반쪽 — 학습 단계가 없으면 같은 L1 안에서 초급·중급을 못 나눈다 |
| ③ 문장·콘텐츠 개선 | 문장ID + 열람·재생·녹음·**건너뜀** | 안 됨 — "안 한 것"이 안 남는다 |
| ④ 원가·전환 분석 | 익명ID별 이벤트 시퀀스 (몇 회에서 결제·이탈) | 안 됨 |

동의: 온보딩에서 별도 체크박스. 거부해도 앱 기능 제한 없음. 거부 시 채점에 필요한 전송만 하고 저장하지 않는다.
저장소: R2. 추정 비용 사용자 1,000명 × 일 10회 기준 월 30GB — 수백 원.
답지 방식: 미결. 후보 셋 — 사람 태깅(비용), Azure 점수를 약한 답지로(정확도 한계), 사용자 재시도 점수 변화를 답지로(가장 싸고 자연스러움, 세션ID가 전제).

~~출시 후 필요할 때 켠다~~ ← 기각 (2026-08-17)
  나중에 켜면 그 전 데이터는 없다. 켜는 비용이 작고 끄는 것은 언제든 가능하다.

~~녹음 파일 단위로 저장~~ ← 기각 (2026-08-17)
  위 표의 ①③④가 안 된다. 나중에 이벤트로 바꾸면 그 전 데이터는 묶을 수 없다.

### 16.2 타일은 "구간"이다 — 단어에서 음절로의 버전업을 전제

첫 버전 화면은 단어 단위 타일(15.9). 그러나 코드에서 타일의 단위를 "단어"로 고정하지 않고
**"구간(segment)"으로 추상화**한다. 첫 버전은 구간=Azure Words 배열의 단어, 이후 음절 표시가 열리면 구간=음절.
화면 구조·색 농도 계산·공유 카드 생성이 구간 수와 길이에 무관하게 동작해야 한다.
근거: 음절 단위는 폐기가 아니라 보류다(15.9). 버전업 때 화면을 다시 짜지 않기 위함.

타일은 참조 문장의 띄어쓰기가 아니라 **Azure 응답의 Words 배열대로** 그린다 (기존 결정 — 단어 경계 불일치 대응).

### 16.3 미결

| 항목 | 막고 있는 것 | 해소 조건 |
|---|---|---|
| 로그 답지 방식 | 음절 모델 재학습 착수 | 실사용 로그 확보 후 판단 |
| 이벤트 저장소 (KV vs 경량 DB) | 구현 | 이벤트 수·질의 패턴 보고 결정. 첫 버전은 KV로 시작해도 됨 |

### 16.4 온보딩 — 모국어 + 학습 단계 자가 신고 (2026-08-17 확정)

온보딩에서 받는 것: **모국어**(기존 결정 — UI 언어·오류 사전 우선순위·백분위 집단을 한 번에 정함) + **학습 단계 자가 신고**(초급/중급/상급, 질문 하나).

근거: 16.1 표의 ②. 같은 L1이라도 학습 단계에 따라 오류 패턴이 다르다. 온보딩에서 안 받으면 나중에 나눌 수 없다.

프로젝트 지침 "로그인·온보딩·레벨테스트를 앞에 두지 않는다"와의 관계:
레벨테스트가 아니라 **자가 신고 한 번**이다. 모국어 질문 옆에 하나 더 붙는 정도라 "첫 화면이 곧 기능" 취지를 크게 해치지 않는다.
건너뛰기 허용. 건너뛰면 "미신고"로 저장한다.

~~학습 단계를 점수 이력에서 추론~~ ← 기각 (2026-08-17)
  첫 채점 전에는 추론할 근거가 없고, 추론값은 자가 신고와 성격이 달라 섞이면 ②의 분석이 흐려진다.
  둘 다 저장하되 자가 신고가 기준.

### 16.5 개인정보처리방침 — 수집 항목 명시 (2026-08-17)

이벤트 로그는 음성보다 범위가 넓다(행동 기록). 방침에 열거해야 하는 항목:
- 음성 데이터 (채점 목적 전송, 동의 시 서비스 개선 목적 저장)
- 사용 기록 (문장 열람·재생·녹음·건너뜀 등 앱 내 행동, 동의 시 저장)
- 모국어·학습 단계 (자가 신고)
- 익명 기기 식별자 (17.3 UUID)
- 국외 이전: Cloudflare(엣지·R2), Microsoft Azure(Korea Central 시작). 17절 기준

법이 막는 것이 아니라 **적어야 하는 것**이다. 배포된 docs/privacy가 이 항목들을 담고 있는지 CC가 확인한다(16.3 미결).

→ 2026-08-17 개정본 배포(ko·en, 빌드 0.1.13). 앱 내 링크·온보딩 동의 체크박스·설정 화면(식별자 보기·삭제 요청)은 미구현 — 구현 단계에서.

### 16.6 보관·삭제 정책 (2026-08-17 확정)

| 항목 | 결정 | 근거 |
|---|---|---|
| 보관 기간 | 동의받은 음성·이벤트 **5년**. 이후 파기 또는 익명화하여 통계 목적으로만 보존 | 앱이 오래갈수록 초기 데이터가 희소 자산이 된다. 특히 소수 L1의 초기 샘플. 무기한은 동의·심사에서 설명이 어렵고, 5년은 "발음 평가 모델의 지속적 개선"으로 설명 가능 |
| 삭제 요청 경로 | 앱 설정에서 "내 식별자 보기 + 삭제 요청". support@naruve.app으로 UUID와 함께 접수. 첫 버전은 수동 처리 | 익명 UUID 체계라 사용자가 자기 식별자를 알아야 요청 가능. 월 몇 건 수준이면 수동으로 충분 |
| 삭제 범위 | 음성 파일·이벤트 로그 삭제. **이미 학습에 반영된 모델과 익명 통계는 제외** | 모델은 원본을 담고 있지 않고 한 사람 데이터를 모델에서 빼는 것은 사실상 불가능. 방침에 명시한다 |
| UUID 보관 | 무료 크레딧 카운터가 존속하는 동안. 방침의 "1년" 기준 폐기 | 1년 뒤 UUID를 지우면 30회 카운터가 초기화된다. 정책이 우연이 되어선 안 된다 |
| 학습 사용 플래그 | 이벤트에 "학습에 사용된 모델 버전" 플래그. 재학습마다 갱신 | 파기·삭제 요청 시 "귀하의 음성은 모델 vN 학습에 사용되었으며 원본은 삭제했습니다"라고 답할 수 있어야 한다 |

**구현 원칙 — 저장 키는 UUID가 앞.**
R2: `{UUID}/{날짜}/{세션}/{녹음}.wav`. KV: `{UUID}:...`.
"이 UUID로 시작하는 것 전부"로 삭제가 한 줄이 된다. 나중에 못 바꾼다 — 이미 쌓인 파일은 그 이름 그대로다.

~~보관 3년~~ ← 기각 (2026-08-17)
  동의받기 쉬운 선이었으나 자산 관점에서 짧다. 삭제 요청을 쉽게 만드는 것으로 동의 부담을 상쇄한다.

~~UUID 1년 후 파기 (08-06 방침)~~ ← 기각 (2026-08-17)
  30회 카운터와 충돌.

## 17. 채점 경로와 키 구조 (2026-08-17 확정)

### 17.1 결정 — 서버 프록시 (A)

앱은 음성을 우리 함수(Cloudflare Workers)로 보내고, 함수가 Azure를 호출해 결과를 돌려준다.
Azure 구독키는 Workers 환경변수에만 둔다. 앱 코드·배포물에 키가 들어가지 않는다.

근거:
- 웹앱·Capacitor 웹뷰 모두 클라이언트 키는 개발자도구로 즉시 노출된다. 난독화는 시간을 조금 벌 뿐이다
- 키가 새면 남의 호출이 우리 청구서에 찍힌다. 전략서 6장의 원가 통제가 통째로 무력화된다
- 16.1(로그 저장)이 확정돼 음성이 어차피 서버로 가야 한다. 프록시면 채점과 저장이 한 경로다
- 무료 30회는 서버가 세야 실제 제한이다. 클라이언트 카운터는 저장소 초기화로 우회된다

~~C. 클라이언트에 키~~ ← 기각 (2026-08-17)
  유출 즉시 원가 통제 붕괴. 30회 제한도 무의미해진다.

~~B. 토큰 교환 (서버가 단기 토큰 발급, 앱이 Azure 직접 호출)~~ ← 기각 (2026-08-17)
  키는 안전하나 (1) 로그 저장을 위해 음성을 따로 또 보내야 해 이중 전송,
  (2) 토큰 유출 시 유효 시간(10분) 동안 무제한. B가 이기는 조건은 "음성을 서버에 안 보내는 것이
  요건일 때"뿐인데 16.1로 그 조건이 사라졌다.

### 17.2 서버 — Cloudflare Workers + R2 + KV

| 역할 | 서비스 | 근거 |
|---|---|---|
| 함수 | Workers | 무료 티어 일 10만 요청. 엣지 실행이라 사용자↔함수 구간이 어디서든 짧다 |
| 음성·결과 저장 | R2 | 이미 계정·결제 있음(COMPANY.md 7절 — 저장소 밖 문서, 프로젝트 지식·개인 폴더에만 있음). Workers와 같은 대시보드, 둘 사이 전송 무료 |
| 횟수 관리 | KV | 익명 ID별 카운터. 무료 티어 있음 |

**원칙: Workers는 전달만 한다.** 음성 처리(리샘플·인코딩·무음 절단)는 전부 클라이언트에서 끝낸다.
Workers 무료 티어는 요청당 CPU 10ms 제한이 있어 서버에서 음성을 손대면 넘는다.
현재 mic.js가 이미 16kHz WAV까지 만들고 있어 이 원칙은 지켜진다.

비용: Workers 유료 전환점은 일 10만 요청(1인 10회 기준 약 1만 명). 유료도 월 $5부터.
Azure 원가($1.32/h)에 비하면 오차 범위. 전략서 6장 원가 모델은 이 결정으로 흔들리지 않는다.

락인: Workers 코드는 표준 fetch 핸들러, R2는 S3 호환, KV는 단순 키-값. 옮기는 데 반나절.
되돌리기 어려운 결정이 아니다.

~~Vercel Functions~~ ← 기각 — 저장소 짝이 없어 외부 서비스가 하나 더 필요
~~AWS Lambda~~ ← 기각 — 설정 복잡도. 1인 운영에서 새 클라우드를 늘릴 이유가 없다

**서버가 지키는 것 (2026-08-19 추가).** 실호출에서 나온 방어 규칙이다.

| 규칙 | 근거 |
|---|---|
| 전 단어가 `Omission`이면 **422 `nothing_recognized`**, R2·KV 무변경 | 무음 WAV와 무작위 바이트가 둘 다 200 / 점수 0 / `RecognitionStatus: Success`로 왔다. 상태 코드로는 갈리지 않아 `Words`를 직접 본다. 하나도 못 알아들은 시도에 크레딧을 깎지 않는다 |
| `NBestPhonemeCount: 5` | 8.7의 2순위 음소 점수. **요청해야 온다** |
| 참조 텍스트 **120자 상한**, 초과 시 400 | R2 `customMetadata` 총량이 약 2KB다. 상한이 없으면 긴 헤더 하나가 Azure 호출을 태우고 R2 put에서 터진다 — 실제로 그렇게 터졌다 |
| 본문 400KB 초과 시 **413 `too_large`** | mic.js가 16kHz/16bit/mono·10초 상한이라 320KB가 최대다. 헤더로 먼저 거른다 |
| CORS 허용 origin은 `https://naruve.app` **하나** | 추측으로 늘리지 않는다. APK에서 실제로 무엇이 오는지 실측한 뒤 더한다 |
| **402는 Azure 호출 전에 돌려준다** | 소진 상태에서 서버를 불러도 Azure 비용이 0이다. 실측 0.41초(Azure 경로는 1~3초). 17.3이 이 성질에 기댄다 |

### 17.3 사용자 식별 — 익명 UUID

앱 첫 실행 시 UUID를 만들어 저장하고, 서버는 그 ID별로 횟수를 KV에 센다. 로그인 없음.

한계를 감수한다: 앱 재설치·저장소 초기화로 새 ID가 나와 30회가 초기화된다.
막으려면 로그인 또는 기기 지문이 필요한데 둘 다 첫 버전에서 뺐다.
우회 비용은 30회 × 회당 약 4~5원(4.2 원가 기준) = 약 120~150원.
(2026-08-17 정정: 초안은 전략서 6.3의 '30회 전체 원가 84원'을 회당으로 잘못 읽어 2,520원으로 적었다.
84원은 30회 합계이며, 유료 전환 시 포기하는 매출 개념이 아니라 순수 API 원가다.)
30회를 다 쓰고 재설치하는 사람은 결제 의사가 있을 가능성이 높다.
결제를 붙이면 결제 계정이 자연스럽게 ID가 된다.

**소진 판정의 권위는 서버다 (2026-08-19 확정).**

~~클라이언트가 캐시된 잔여가 0이면 서버를 부르지 않는다~~ ← **변경 (2026-08-19).**

클라이언트의 `localStorage` 잔여는 **화면에 그릴 숫자일 뿐이고 호출을 막지 않는다.**
녹음이 끝나면 언제나 `/score`를 부르고, **402를 받고서야** 소진 상태로 간다.

근거 — 0.1.18에서 정반대로 했다가 막혔다. 테스터를 18절의 예외 목록에 올렸는데도
폰의 캐시가 0으로 굳어 `/score`를 한 번도 부르지 못했고, 그래서 서버가 주는
"세지 않음" 신호를 받을 길이 없었다. `localStorage`라 새로고침으로도 풀리지 않았다.
**캐시가 서버의 판단을 영구히 가로막는 구조였다.**

비싸지도 않다 — 17.2대로 402는 Azure 호출 전에 온다. 값이 나가는 것은 WAV 업로드뿐이고,
그것도 정말 소진한 사람이 계속 녹음할 때만이다.

세션 단위로 건너뛰는 것(서버가 이번 실행에서 402를 준 뒤에만 생략)은 **P6-B 예정**이다.
세션은 새로고침으로 풀리므로 같은 고착이 생기지 않는다.

### 17.4 Azure 리전 — Korea Central로 시작, 추가 가능 구조

Azure Speech 리소스는 Korea Central로 시작한다.
근거는 개발·테스트 환경과 초기 테스터(안산 공동체·온라인 수강생) 위치이며,
**사용자 시장을 한국으로 보아서가 아니다.**

Workers는 리소스 목록에서 사용자 국가(Cloudflare 요청 헤더 CF-IPCountry)별로 고르는 구조로 짜고,
첫 버전은 목록에 하나만 둔다. 트래픽 분포가 보이면 리전을 추가한다.
리전 추가 = Azure 리소스 하나 더 + 목록에 한 줄. 첫 리전을 바꾸는 것이 아니라 더하는 것이다.

어느 리전을 골라도 지구 반대편은 있다. 왕복 300~400ms 차이는 Azure 자체 처리(1~2초)에 붙는 것이라
"조금 더 기다림"이지 "쓸 수 없음"이 아니다.

**발음평가 리전 지원 (2026-08-18 확인).** 발음평가는 모든 STT 리전에서 지원되며
koreacentral도 포함된다(Microsoft 문서). 키는 리전 종속이므로
**Workers가 부르는 엔드포인트가 리소스 리전과 일치해야 한다.**

### 17.5 L1 샘플 순서 ≠ 시장 순서 (2026-08-17 명문화)

앱 사용자는 첫 버전부터 전 세계 한국어 학습자다. 특정 국가를 초기 시장으로 두지 않는다.
인도네시아어권은 **L1별 오류 사전을 만드는 첫 샘플**이지 시장이 아니다.
이주민 센터의 다국어 화자 접근성으로 L1 샘플을 순차 확장한다.

문서 여러 곳(전략서 v2 4.3·5.1, 프로젝트 지침)의 "1차 인도네시아어+영어", "인도네시아 시장" 표현은
이 구분 이전의 것이다. 충돌하면 이 절이 이긴다. 전략서 v3 개정 시 반영.

### 17.6 사람이 한 일 (2026-08-18 완료)

- **Cloudflare 계정** `c3eda5f2dd5cadf18407852bbe3ac29e`.
  기존 kata 계열 Workers·KV가 있으므로 가나다 자원은 **`naruve-ganada-` 접두어**로 구분한다
- **Workers** 이미 활성화되어 있었다. 가나다 Worker는 대시보드에서 만들지 않고
  CC가 `wrangler deploy`로 생성한다 — 이름·바인딩이 `wrangler.toml` 한 곳에서 정해지도록
- **R2 버킷** `naruve-ganada-audio` 생성. Location Automatic(Asia Pacific), Standard,
  Public Access Disabled. 기존 `kata-bible-audio`는 Kata의 성경 음원이라 공용하지 않는다 —
  사용자 음성은 삭제·보관·원가를 따로 봐야 한다
- **KV 네임스페이스** `naruve-ganada-kv` 생성. ID `7bba6faac92e4a2c9f40e82ddeee025a`
- **Azure Speech** 새로 만들지 않았다. 기존 `naruve-speech`(koreacentral, F0)를 재사용한다.
  키는 `Naruve Pronunciation\.env`에 있다(저장소 밖)
- **Azure 키 → Workers Secret** Worker 첫 배포 뒤 사람이 대시보드에서 등록한다.
  배포 전에는 넣을 자리가 없고, **키를 미리 복사해 두지 않는다**

### 17.7 미결

| 항목 | 막고 있는 것 | 해소 조건 |
|---|---|---|
| Azure F0 → S0 전환 | 동시 요청 수 (F0는 조정 불가) | 비공개 테스트 시작 하루 전 Portal에서 전환. 같은 리소스라 키 유지, 반영에 수 시간 |
| ~~Azure 키 Workers Secret 등록~~ | — | **해소됨 (2026-08-19).** `/health`의 `hasKey`가 `true`다. 값은 어떤 응답에도 실리지 않는다 |
| 예시 음성이 먹먹하고 작게 들린다 | 원인 미확정 | 가설 — `getUserMedia` 스트림을 유지하는 동안 안드로이드가 오디오 경로를 통신 모드로 돌린다. 계측 로그(`mic=live/ended`)를 넣었고 **크롬 vs APK 실험 중**. 결과를 보고 고친다 |

### 17.10 개발용 remote / 출시용 bundled 이원화 (2026-08-19 확정)

*번호는 17.6이 아니라 17.10이다. 17.6~17.9가 이미 쓰이고 있어 다시 번호를 매기면
CLAUDE.md와 코드 주석의 참조가 어긋난다.*

개발 중 APK는 **껍데기**다. Capacitor `server.url = https://naruve.app/` 로 두고
WebView가 배포된 웹을 그대로 띄운다.

- `appName` **"Naruve 가나다 (dev)"**, **debug 서명**. `appId`는 `app.naruve.ganada` 그대로 둔다
  — 바꾸면 Play 계정과 어긋난다
- **웹 수정은 Pages 배포로 반영된다.** APK 재빌드는 `android/`·`capacitor.config.json`·
  플러그인을 고쳤을 때만이다
- 마이크 권한은 **Capacitor 8의 `BridgeWebChromeClient`가 처리한다.** WebView의
  `AUDIO_CAPTURE` 요청을 받아 `RECORD_AUDIO`·`MODIFY_AUDIO_SETTINGS`를 런타임 권한으로
  대신 요청한다. **`MainActivity`에 손댈 것이 없다** (소스 확인)

출시 시에는 뒤집는다 — `server.url` 제거 → `webDir` 번들 → release 서명 → AAB.
원격 URL만 띄우는 앱은 심사에서 반려될 수 있고, 번들로 바꾸면 그때부터 웹 수정마다
APK 재빌드가 필요하다.

### 17.8 Azure 티어와 12개월 원가 추정 (2026-08-18)

**티어.** F0는 STT 계열에 월 5 오디오 시간이 붙고, 초과하면 과금이 아니라 **차단**된다.
S0는 무료 할당 없이 첫 1초부터 종량이고, 발음평가가 시간당 약 **$1.30~1.32**다
(출처에 따라 $1.32 단일 / $1 + 부가 $0.30으로 갈리나 합계는 같다). 과금은 1초 단위.
회당 8초 기준 **약 4.1원**(1,400원/$).
**F0의 실질 제약은 총량(월 2,250회)이 아니라 조정할 수 없는 동시 요청 수다.**

**12개월 추정.** 전제 — 월 신규 설치 500(1~3월) → 1,500(4~6월) → 3,000(7~12월),
전환율 3%, 월 해지 10%, 실수령 5,900원, 회당 8초, 무료 30회 전량 소진,
유료 월 300회, 재채점 없음. **전부 추정치이고 비공개 테스트에서 실측으로 대체한다.**

| 월 | 신규 설치 | 유료 누적 | 월 호출 | Azure 원가 | 매출(실수령) | 순익 | 누적 |
|---|---|---|---|---|---|---|---|
| 1 | 500 | 15 | 20K | 8만 | 9만 | +1만 | +1만 |
| 3 | 500 | 41 | 27K | 11만 | 24만 | +13만 | +21만 |
| 6 | 1,500 | 152 | 91K | 37만 | 90만 | +53만 | +130만 |
| 9 | 3,000 | 355 | 197K | 81만 | 209만 | +128만 | +427만 |
| 12 | 3,000 | 503 | 241K | 99만 | 297만 | +198만 | +958만 |

읽는 법:

- 한 달 단면만 보면 설치당 원가 약 123원 + 유료 3%×1,230원 = **160원 vs 매출 177원**으로
  거의 본전이다. 이것이 총량 30회 방식의 정의다 — **무료 원가는 설치 때 1회,
  구독 매출은 매달 반복.** 수익은 둘째 달부터 난다
- 유료 1인당 남는 돈은 매달 약 **4,700원**(5,900−1,230)이고 규모가 커져도 꺾이지 않는다.
  자체 서버로 옮긴 뒤에는 유료 1인 원가가 거의 0으로 내려가 더 가팔라진다
- **6개월까지는 검증 기간**(누적 130만). 목적은 돈이 아니라
  전환율·해지율·회당 발화 길이의 실측이다
- 표를 무너뜨리는 것 — 전환율 1%면 12개월 순익이 거의 0이다.
  재채점을 무료에 실시간으로 열면 원가가 3~4배가 되어 8개월까지 적자다.
  체감가를 3,900원으로 내리면 12개월 누적이 약 350만이다
- **자체 서버 교차점** (클라우드 GPU 임대, 저가 등급 월 30~50만 추정, 장비 구매 없음):
  Azure 월 원가 ≈ GPU 월세, 즉 월 약 10만 회(유료 200명 + 월 신규 1,300 언저리)로
  6~7개월째다. 전환 작업 기간을 감안해 1.5~2배(월 15~20만 회, 유료 400~500명)인
  **7~9개월째 착수가 안전하다.** 그때까지 Azure에 내는 누적은 약 300만 원이고,
  그 대가는 서버 운영 없이 출시하고 데이터를 모으는 것이다.
  전략서 5.4의 자체 모델 조건(MAU 5만·월 API 200만)은 이 기준으로 대체한다
- 우리 모델(15절)은 원어민다움 점수를 내지 못하므로 고도화되어도 **Azure를 대체하지 않고
  옆에 붙는다.** 첫 해 손익은 모델이 아니라 전환율이 결정한다
- Cloudflare는 이 규모에서 사실상 0원이다 (Workers 일 10만 요청 무료,
  R2는 회당 약 256KB로 월 24만 회도 무료 10GB 초과분이 약 $0.75)

### 17.9 출시 시점과 모델 고도화의 순서 (2026-08-18 확정)

~~출시를 늦추고 우리 모델을 먼저 다듬는다~~ ← 기각 (2026-08-18)
  모델을 올리는 재료는 L1별 사용자 음성 + 라벨이고 **출시 없이는 생기지 않는다.**
  AI-Hub 927시간(영어 화자만)을 더 붙들어도 44%→48%는 되어도 65%는 안 된다.
  늦추면 데이터가 안 쌓이는 개월 수만 잃는다.

- **출시 조건은 지금 CC 파이프라인 그대로다** — Azure 단어 점수 + F0 + 30회 카운터
  + 로그 저장 + 온보딩. **모델은 첫 버전에 없다**(15절 유지)
- 출시 전 모델 작업은 **고정 예산 안에서만** 한다(주 1회 저녁 하나 수준, 출시일 불변).
  후보 둘 — Azure 단어 점수를 GBM 입력 특징으로 추가(AI-Hub 표본 50시간 약 $66,
  8.11.6 확인 필요), 더 큰 음향 모델
- **모델 본격 착수 관문은 날짜가 아니라 데이터 양이다** — 로그된 발화 5,000건 +
  라벨 표본 500건(추정, 실제 로그 속도를 보고 조정). 관문이 열리면 L1별 모델 →
  **65%를 넘는 L1부터** 오류 설명을 개방한다

---

## 18. 무료·유료 경계 (2026-08-18 확정)

| 기능 | 무료 | 유료 |
|---|---|---|
| 정밀 채점 | **30회 총량** (설치당 1회 지급) | **월 1,500회 상한**, 초과 시 강등 |
| 단어 점수 + 두루뭉실 설명 (15.10) | O | O |
| F0 억양 | 방향만 | 곡선 비교 (내 것 vs 예시) |
| 컬렉션 | 전부 (Drama 포함) | 전부 |
| 진행 기록 | 없음 | 문장별 최고점·시간 추이 |
| 결과 카드 공유 | O | O |
| 오류 확정 진단 | — | 미정 (4절 소거법 확인 후) |

**유료가 사는 것은 횟수다.** 나머지는 "쓸수록 더 보인다"를 채우는 것이고,
F0 곡선·진행 기록은 **원가 0으로 체감 차이를 만드는 자리**다.

~~컬렉션 잠금~~ ← 기각 (2026-08-18)
  30회 총량이 이미 제한이다. 재밌는 쪽(Drama)을 무료에서 경험시키는 것이 바이럴에 이득이다.

~~유료 일 30회 상한 (전략서 6.5)~~ ← 기각 (2026-08-18)
  "월 $4.99에 하루 30번"은 구독 유인을 죽인다. 대신 **월 1,500회**로 바꾼다.
  근거 — 회당 4.1원 × 1,500 = 6,150원으로 실수령 5,900원의 본전 지점이다.
  넘는 사람은 우리 원가로 채점받는 사람이다. 꼬리(헤비 유저)의 적자에 상한을 두되
  **사용자가 평소엔 존재를 모르게 한다** — 앱 어디에도 미리 노출하지 않고
  초과 시점에만 안내한다.

~~유료 상한 완전 폐지, 소량 사용자와 상쇄~~ ← 기각 (2026-08-18)
  상쇄는 평균에서 성립하고 문제는 꼬리다. 유료 100명 중 5명이 월 3,000회면
  그 5명의 적자를 나머지가 매달 메운다. 발음 연습은 반복 행위라 꼬리가 두꺼울
  가능성이 있다. **재정 여력이 생기면 다시 열 수 있는 결정이다.**

- 초과 시 강등 문구 — "이번 달 정밀 채점을 다 쓰셨어요. 다음 달 1일에 다시 채워집니다."
  강등 후에도 듣기·녹음·F0 억양은 유지한다
- 무료 30회 소진 후 — 전략서 6.3의 "기본 모드(온디바이스)"는 **첫 버전에 없다.**
  첫 버전은 듣기(TTS)·녹음(점수 없음)·F0 억양만 남긴다.
  원가 0인 F0가 있어 "죽은 앱"은 안 된다
- 월 1,500회·표본 비율·30회 소진 후 상태는 **비공개 테스트에서 유료 사용 분포
  (중앙값·상위 5%)를 보고 조정한다**

### 18.1 무료 기간·쿠폰 (2026-08-18 일부 확정)

~~출시 첫 달 무제한 무료~~ ← 기각 (2026-08-18)
  총량 방식의 존재 이유가 **설치당 원가 상한(약 123원)**인데 무제한이면 그 상한이 사라진다.
  바이럴이 실제로 터질수록(목표인데) 매출 0인 달에 청구서가 터진다 —
  5만 설치 × 60회 × 4원 = **1,200만 원**. 성공할수록 위험한 구조라 채택하지 않는다.

- 첫 달 설치자에게 넉넉히 지급(예: 60회)하는 것, SNS 공유 시 추가 회수 쿠폰
  (공유 버튼 = 즉시 지급, 상한 있음)은 **출시 전 재논의**로 미룬다.
  **지금 확정된 것은 "무제한은 없다"뿐이다**
- 참고 — Play 비공개 테스트 14일 + 수익화 4~6주 뒤(전략서 5.2·5.4)이므로
  첫 4~6주는 어차피 결제가 없고 30회 총량이다

### 18.2 테스터 예외 — DEV_UUIDS (2026-08-19 확정)

무료 30회는 설치당 총량이라 **테스터가 하루에 소진한다.** 그렇다고 총량을 늘리면
18절의 존재 이유인 설치당 원가 상한이 사라진다. 그래서 **특정 UUID만 세지 않는다.**

`DEV_UUIDS` — 콤마로 구분한 UUID 목록. **Workers Secret이다.**
`wrangler.toml`의 `[vars]`에 넣지 않는다. 값 자체는 익명 식별자라 비밀이 아니지만,
이 목록에 오르는 것은 **"과금 없이 무제한"이라는 권한**이라 저장소에 남기지 않는다.

| | 목록에 있을 때 | 없을 때 |
|---|---|---|
| KV 읽기·차감 | **하지 않는다** | 한다 |
| 402 | **없다** | 잔여 0이면 |
| 응답 `credits` | **`-1`** → 헤더에 `∞` | 남은 수 |
| R2 저장 · 로그 | **그대로 한다** | 한다 |

테스터 음성도 데이터다. 16.1이 채점결과 페이로드를 남기라고 한 대상에서 빠질 이유가 없다.

**Secret은 덮어쓰기다.** 한 명을 더할 때 그 사람만 넣으면 나머지가 지워진다 —
언제나 **목록 전체를 다시 등록한다.**

### 18.3 소진 후 상태 — 실기 확인 (2026-08-19)

30회를 다 쓴 상태를 실기로 확인했다. **18절 설계대로 동작한다.**

- 듣기(예시 음성) · 녹음 · F0 억양 — **유지**
- 정밀 채점만 막히고 안내 문구가 뜬다
- "죽은 앱"이 되지 않는다는 전제가 실물로 확인된 것이다

### 18.4 첫 버전 화면 (2026-08-19 확정)

무료·유료와 직접 관계는 없지만, 18절이 "무료 사용자가 보는 화면"을 규정하므로
여기 함께 적는다. 개념상으로는 16.2(타일 = 구간)의 이웃이다.

- **마이크 도크** — 탭바 가운데. 5칸(말 · 높 · **[마이크]** · 나 · 설정)에서
  가운데 칸을 비우고 그 위에 얹는다. 버튼 지름 **74px**
- ~~`.record-zone` (화면 아래 별도 행)~~ ← **폐기.** 화면 아래 1/3을 먹어
  결과가 한 화면에 들어오지 않았다
- **타일 색** — 먹물 검정 → **진청 `#172554`**. 배경 alpha = `0.12 + score/100 × 0.84`,
  **글자 반전 임계 58**. 임계는 색마다 다시 재야 한다 — WCAG 상대휘도로 잰
  교차점이 검정 54.4 / `#1E3A8A` 65.8 / `#172554` 57.5였다
- `l1box`(확정 오류 설명) 호출은 **차단**한다 — 15.10대로 정밀도 65%를 넘긴
  L1부터 켠다. 코드와 데이터는 남겨 둔다
- **온보딩에 "따라 할 목소리 남/여" 문항을 더한다 (P6-B 예정).** 9.2의 두 보이스
  중 자기 음역대를 고르게 한다. 16.4의 두 문항(모국어·학습 단계) 옆에 붙는다

---

## 19. Play 스토어 등록 (2026-08-24 확정)

### 19.1 단일 앱 — 언어권별 분리는 기각

~~언어권별로 앱을 따로 낸다 (일본용·인도네시아용·대만용)~~ ← 기각 (2026-08-24)

- **Play 스팸 정책의 중복 앱 금지에 걸릴 수 있다.** 내용이 거의 같은 앱을
  여러 개 올리는 형태가 된다
- **순위·평점·다운로드는 앱 단위로 쌓인다.** 나누면 그 축적이 갈라진다
- **아키텍처가 이미 단일 앱을 전제한다.** 온보딩의 모국어 선택 하나가
  UI 언어·오류 가설 사전·백분위 비교군을 함께 정한다 (7절 · 16.4)
- **언어권 구분은 언어별 스토어 등록정보로 해결된다.** 앱을 나눌 이유가 아니다

아이콘도 하나다. 언어권별 아이콘을 만들지 않는다.

### 19.2 패키지명 app.naruve.ganada (확정)

`appId` = `app.naruve.ganada`.

**최초 AAB 업로드 시 영구 고정된다.** 바꾸려면 새 앱이 되고 순위·평점·설치 수를
처음부터 다시 쌓는다. 그 비가역을 알고 확정한다. 17.10이 개발용 빌드에서도
`appId`를 그대로 두라고 한 이유가 이것이다.

### 19.3 배포 형태 — 무료 앱 + 인앱 결제 (확정)

무료 앱으로 등록하고 크레딧을 인앱 결제로 판다 (18절).

**무료로 등록한 앱은 나중에 유료 앱으로 바꿀 수 없다.** 그 비가역을 알고 무료를
고른다 — 18절의 경계가 이미 "설치는 공짜, 파는 것은 횟수"이므로 유료 앱 등록은
애초에 선택지가 아니다.

### 19.4 스토어 등록정보 — 기본 언어 en-US (확정)

기본 언어는 **en-US**다. 추가 언어 등록정보는 이후 확장한다.

| 항목 | 값 | 길이 |
|---|---|---|
| 앱 이름 (en) | `Naruve Ganada: K-Pronunciation` | 30자 (한도 30, 정확히 충족) |
| 짧은 설명 (en) | `Get a Korean pronunciation score and see how native-like you sound.` | 67자 (한도 80) |

**둘은 묶음이다.** 짧은 설명에 `Korean pronunciation`을 **온전한 형태로 반드시
포함한다.** 제목의 `K-Pronunciation`이 조어라서 검색어 "korean pronunciation"
대비 불리할 가능성이 있고, **불리한 정도는 알 수 없다** — 설명에서 회수한다.
제목만 보고 고치면 이 회수가 조용히 사라진다.

~~"Korean Pron."~~ ← 기각 (2026-08-24)
  축약이 잘린 인상을 주고, pron이 속어를 연상시킨다.

~~"Korean Score"~~ ← 기각 (2026-08-24)
  TOPIK 같은 종합 점수로 오독될 수 있다.

ChatGPT 교차 검증을 거쳤다.

### 19.5 연락 이메일 support@naruve.app (확정)

Play 스토어에 표시되는 연락처다. 지금은 **수신 전용**이고
(Spaceship 포워딩 → 개인 지메일) 발신 설정은 아직 없다.
출시 전 할 일에 있다 — CLAUDE.md "출시 전 반드시 할 일".

---

## 변경 이력

이 절은 2026-08-09에 신설되었다. 그 이전 이력은 git log를 본다.

- 2026-08-09 — 띄어쓰기 실측 반영. 8.8 신설 / 8.6-3 정정 / 8.6 음절 타일 기본 보류 / 0절 (b) 부정 증거 / 3.2 · 4.3 · 8.5 · 12 · 14 갱신
- 2026-08-10 — 18문장 40회 실측 반영. 8.9 신설 / 3.2 원어민 눈금 재측정 / 4.3 전제 3 부정 실측 / 0절 (b) 부정 증거 2건 추가 / 8.4 · 8.5 · 8.6 · 8.8 · 14 갱신
- 2026-08-10 — ETRI 발음평가 API 사양 확인 후 탈락 기록(응답이 1~5점 하나뿐). 8.2 · 8.4 · 14 갱신
- 2026-08-10 — REST 실측 34회 반영. 8.10 신설(EnableMiscue) / 8.9 C군 13번 화면 값 정정 · REST 재측정 추가 / 8.8 확정 4 반증 · 방향성 블록 손질 / 3.2 Studio vs REST · SNR 서술 정정 / 0절 (b) · 14 갱신
- 2026-08-12 — AI-Hub 검증 사흘(08-10~12) 반영.
  3.4 신설(감점 원칙과 결정 6+4) / 8.11 신설(AI-Hub 데이터·억양·대응표) /
  8.12 신설(GOP→학습 판별기, top-1 34%, 규모 곡선, 판단) /
  0절 (b) 갱신 / 8.2 후보 추가 / 14 갱신. 폐기 지표 6종 기록.
- 2026-08-13 — 신형 음향 특징 검증 반영. 8.13 신설(후보 조사·5k 비교·
  전량 top-1 44.1%·GPU 36.5배 실측·판단) / 0절 (b) 갱신 / 8.2 행 교체 /
  14 갱신. 모델 ② 사용 금지 확정. WSL D 이전 기록.
- 2026-08-16 — 15절 신설 — 음절 표시 하한선(정밀도 75/top-1 65), 판정 기준선 A-1,
  이유 문구 첫 버전 제외, GBM 필수, 클라우드 조건부.
  8.12.6·8.13.6에 15.3 참조 추가.
- 2026-08-17 — 15절 보강 — 기준선 재정의(재현 불가 실증), 부트스트랩 결과,
  세 결정(미세조정 닫음·클라우드 없음·단어 단위 확정)
- 2026-08-17 — 16절 신설 — 로그 저장 첫 버전부터, 타일=구간 추상화. 15.2 괄호 정리
- 2026-08-17 — 17절 신설 — 채점 경로 서버 프록시(Workers+R2+KV), 익명 UUID,
  Azure Korea Central 시작·추가 구조, L1 샘플≠시장
- 2026-08-17 — 17.3 우회 비용 정정(2,520원 → 120~150원), 17.2 COMPANY.md 참조에 저장소 밖 표기
- 2026-08-17 — 16절 보강 — 로그 저장 단위를 이벤트로, 용도 4종에서 역산한 필드,
  온보딩 학습 단계 자가 신고, 방침 사용 기록 수집 명시
- 2026-08-17 — 16.6 신설 — 보관 5년, 앱 내 삭제 요청, 삭제 범위(모델·통계 제외),
  UUID 카운터 존속, 저장 키 UUID 앞
- 2026-08-17 — 방침 개정본 배포(ko·en) 반영 — 16.3 미결에서 '개인정보처리방침 문구' 행 제거,
  16.5에 배포 결과와 남은 미구현 항목 한 줄
- 2026-08-18 — 4절 재채점 실측 미작동(4/4)·소거법 미결·오프라인 라벨 용도로 이동,
  15.10 두루뭉실 설명 층, 18절 무료·유료 경계(월 1,500회 상한·컬렉션 전부 개방·
  일 30회 상한 기각·무제한 무료 기간 기각), 17.6 완료, 17.7 갱신, 17.4 리전 확인,
  17.8 티어·12개월 원가 추정·자체 서버 교차점, 17.9 출시·모델 순서. 0절 관문 한 줄 정리
- 2026-08-19 — 9절 9.1~9.5 신설(타입캐스트 확정·ElevenLabs 기각·글자당 1크레딧
  오독 정정·보이스 우성/이현·seed 비재현·손질 절차·앱 배치·API 손잡이 한계),
  15.10.1 신설(억양 불일치 −15, SCORE_MIX 폐기, "통합 비율 재검토" 닫음),
  17.2 서버 방어 규칙 / 17.3 소진 판정 권위 / 17.7 미결 갱신(Secret 해소·먹먹함 추가) /
  17.10 신설(개발 remote·출시 bundled 이원화), 18.2~18.4 신설(테스터 예외 DEV_UUIDS·
  소진 후 실기 확인·첫 버전 화면과 타일 색)
- 2026-08-24 — 19절 신설 — Play 스토어 등록(단일 앱 확정·언어권 분리 기각,
  패키지명 app.naruve.ganada 영구 고정, 무료 앱 + 인앱 결제, 기본 언어 en-US,
  영문 앱 이름·짧은 설명 확정과 묶음 조건, 연락 이메일 support@naruve.app)
